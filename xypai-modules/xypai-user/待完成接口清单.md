# XyPai-User 待完成接口清单

> **版本**: v1.0.0
>
> **创建日期**: 2025-11-28
>
> **目标**: 明确前端 Profile 模块所需的后端接口，包含请求/响应 JSON 规范

---

## 📋 接口概览

### 接口完成状态 (2025-11-28 更新)

| 分类 | 接口数量 | 已完成 | 待实现 | 优先级 |
|------|----------|--------|--------|--------|
| 用户资料接口 | 17 | 14 | 3 (需聚合) | P0 |
| 社交关系接口 | 7 | 7 | 0 | ✅ 已完成 |
| 技能管理接口 | 10 | 10 | 0 | ✅ 已完成 |

### 🔴 前端需直接调用 xypai-content 的接口

以下3个接口数据来源于 **xypai-content** 模块，前端应直接调用 xypai-content 服务：

| 原接口 (xypai-user) | 替换为 (xypai-content) | 当前状态 |
|---------------------|------------------------|----------|
| ~~`GET /profile/posts`~~ | `GET /xypai-content/api/v1/content/feed/user/{userId}` | ⚠️ **待实现** |
| ~~`GET /profile/favorites`~~ | `GET /xypai-content/api/v1/interaction/collect/my` | ⚠️ **待实现** |
| ~~`GET /profile/likes`~~ | `GET /xypai-content/api/v1/interaction/like/my` | ✅ **已存在** |

> **前端注意**:
> - xypai-user 中的这3个接口目前返回空数据
> - `/interaction/like/my` 接口已在 xypai-content 实现
> - `/content/feed/user/{userId}` 和 `/interaction/collect/my` 需要后端在 xypai-content 中实现

### ⚠️ 需要完善的接口

| 接口 | 当前状态 | 待完善内容 |
|------|----------|------------|
| `POST /profile/avatar/upload` | Stub | 需要集成 OSS 文件上传 |

---

## 🔴 优先级 P0 - 用户资料接口

### 1. 获取编辑资料数据

**接口说明**: 编辑资料页面加载时调用，获取当前用户完整资料

**请求**
```http
GET /xypai-user/api/user/profile/edit
Authorization: Bearer <token>
```

**响应 JSON**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "userId": 10001,
    "nickname": "小美探店",
    "avatar": "https://cdn.example.com/avatar/10001.jpg",
    "gender": "female",
    "birthday": "1995-06-15",
    "residence": "广东省广州市天河区",
    "height": 165,
    "weight": 50,
    "occupation": "设计师",
    "wechat": "xiaomei666",
    "bio": "热爱生活，分享美好",
    "isOnline": true,
    "stats": {
      "followingCount": 128,
      "fansCount": 1024,
      "likesCount": 5000,
      "postsCount": 30,
      "collectionsCount": 100,
      "skillsCount": 3
    },
    "privacy": {
      "showAge": true,
      "showHeight": true,
      "showWeight": false
    }
  }
}
```

**字段说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| userId | long | 是 | 用户ID |
| nickname | string | 是 | 昵称，2-20字符 |
| avatar | string | 否 | 头像URL |
| gender | string | 否 | 性别: `male`, `female`, `other`, `null` |
| birthday | string | 否 | 生日，格式: YYYY-MM-DD |
| residence | string | 否 | 居住地 |
| height | integer | 否 | 身高(cm)，100-250 |
| weight | integer | 否 | 体重(kg)，30-200 |
| occupation | string | 否 | 职业 |
| wechat | string | 否 | 微信号 |
| bio | string | 否 | 个性签名，最多200字符 |
| isOnline | boolean | 是 | 是否在线 |
| stats | object | 是 | 统计数据 |
| privacy | object | 否 | 隐私设置 |

---

### 2. 获取个人主页头部信息

**接口说明**: 个人主页加载时调用，获取头部展示数据

**请求**
```http
GET /xypai-user/api/user/profile/header
Authorization: Bearer <token>
```

**响应 JSON**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "userId": 10001,
    "nickname": "小美探店",
    "avatar": "https://cdn.example.com/avatar/10001.jpg",
    "backgroundImage": "https://cdn.example.com/bg/10001.jpg",
    "gender": "female",
    "age": 28,
    "bio": "热爱生活，分享美好",
    "isOnline": true,
    "isVerified": true,
    "verifiedType": "official",
    "stats": {
      "followingCount": 128,
      "fansCount": 1024,
      "likesCount": 5000
    },
    "tags": ["探店达人", "美食博主"],
    "level": 5,
    "memberType": "vip"
  }
}
```

**字段说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| userId | long | 是 | 用户ID |
| nickname | string | 是 | 昵称 |
| avatar | string | 否 | 头像URL |
| backgroundImage | string | 否 | 背景图URL |
| gender | string | 否 | 性别 |
| age | integer | 否 | 年龄（根据生日计算，尊重隐私设置） |
| bio | string | 否 | 个性签名 |
| isOnline | boolean | 是 | 是否在线 |
| isVerified | boolean | 否 | 是否认证 |
| verifiedType | string | 否 | 认证类型: `official`, `creator`, `merchant` |
| stats | object | 是 | 统计数据 |
| tags | array | 否 | 用户标签 |
| level | integer | 否 | 用户等级 |
| memberType | string | 否 | 会员类型: `normal`, `vip`, `svip` |

---

### 3. 获取他人主页信息

**接口说明**: 查看他人主页时调用

**请求**
```http
GET /xypai-user/api/user/profile/other/{targetUserId}
Authorization: Bearer <token>
```

**路径参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| targetUserId | long | 是 | 目标用户ID |

**响应 JSON**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "userId": 10002,
    "nickname": "游戏达人",
    "avatar": "https://cdn.example.com/avatar/10002.jpg",
    "backgroundImage": "https://cdn.example.com/bg/10002.jpg",
    "gender": "male",
    "age": 25,
    "bio": "王者荣耀大神",
    "isOnline": true,
    "isVerified": false,
    "stats": {
      "followingCount": 50,
      "fansCount": 2000,
      "likesCount": 10000,
      "skillsCount": 5
    },
    "followStatus": "none",
    "isBlocked": false,
    "canViewProfile": true,
    "canViewMoments": true,
    "canViewSkills": true,
    "tags": ["游戏主播", "王者荣耀"],
    "skills": [
      {
        "skillId": 5001,
        "skillName": "王者荣耀陪玩",
        "coverImage": "https://cdn.example.com/skill/wzry.jpg",
        "price": 30.00,
        "priceUnit": "局",
        "rating": 4.8
      }
    ]
  }
}
```

**字段说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| followStatus | string | 是 | 关注状态: `none`, `following`, `followed`, `mutual` |
| isBlocked | boolean | 是 | 是否已拉黑该用户 |
| canViewProfile | boolean | 是 | 是否可查看资料 |
| canViewMoments | boolean | 是 | 是否可查看动态 |
| canViewSkills | boolean | 是 | 是否可查看技能 |
| skills | array | 否 | 用户技能列表（最多显示3个） |

---

### 4. 获取个人资料详情

**接口说明**: 点击"资料"Tab 时调用

**请求**
```http
GET /xypai-user/api/user/profile/info
Authorization: Bearer <token>
```

**响应 JSON**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "userId": 10001,
    "nickname": "小美探店",
    "avatar": "https://cdn.example.com/avatar/10001.jpg",
    "gender": "female",
    "birthday": "1995-06-15",
    "age": 28,
    "constellation": "双子座",
    "residence": "广东省广州市天河区",
    "hometown": "湖南省长沙市",
    "height": 165,
    "weight": 50,
    "occupation": "设计师",
    "education": "本科",
    "school": "中山大学",
    "company": "某科技公司",
    "bio": "热爱生活，分享美好",
    "interests": ["美食", "旅行", "摄影"],
    "skills": [
      {
        "skillId": 5001,
        "skillName": "探店达人",
        "skillType": "offline",
        "coverImage": "https://cdn.example.com/skill/explore.jpg"
      }
    ],
    "certifications": [
      {
        "type": "identity",
        "status": "verified",
        "verifiedAt": "2025-01-15"
      }
    ]
  }
}
```

---

### 5. 获取动态列表

**接口说明**: 点击"动态"Tab 时调用

**请求**
```http
GET /xypai-user/api/user/profile/posts?page=1&pageSize=20
Authorization: Bearer <token>
```

**查询参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | integer | 否 | 1 | 页码 |
| pageSize | integer | 否 | 20 | 每页数量，最大50 |

**响应 JSON**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "postId": 1001,
        "content": "今天发现一家超好吃的火锅店！环境很棒，服务也很好~",
        "images": [
          "https://cdn.example.com/post/1001_1.jpg",
          "https://cdn.example.com/post/1001_2.jpg"
        ],
        "video": null,
        "location": {
          "name": "海底捞火锅(天河城店)",
          "address": "广州市天河区天河路123号"
        },
        "topics": [
          {
            "topicId": 101,
            "topicName": "美食探店"
          }
        ],
        "likeCount": 128,
        "commentCount": 32,
        "shareCount": 10,
        "isLiked": false,
        "isCollected": true,
        "createdAt": "2025-11-27 10:30:00",
        "visibility": "public"
      }
    ],
    "total": 50,
    "page": 1,
    "pageSize": 20,
    "hasMore": true
  }
}
```

**字段说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| postId | long | 是 | 动态ID |
| content | string | 是 | 动态内容 |
| images | array | 否 | 图片URL列表，最多9张 |
| video | object | 否 | 视频信息 |
| location | object | 否 | 位置信息 |
| topics | array | 否 | 关联话题 |
| likeCount | integer | 是 | 点赞数 |
| commentCount | integer | 是 | 评论数 |
| shareCount | integer | 是 | 分享数 |
| isLiked | boolean | 是 | 当前用户是否已点赞 |
| isCollected | boolean | 是 | 当前用户是否已收藏 |
| createdAt | string | 是 | 发布时间 |
| visibility | string | 是 | 可见性: `public`, `friends`, `private` |

---

### 6. 获取收藏列表

**接口说明**: 点击"收藏"Tab 时调用

**请求**
```http
GET /xypai-user/api/user/profile/favorites?page=1&pageSize=20
Authorization: Bearer <token>
```

**响应 JSON**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "favoriteId": 2001,
        "targetType": "post",
        "targetId": 1001,
        "post": {
          "postId": 1001,
          "content": "今天发现一家超好吃的火锅店！",
          "coverImage": "https://cdn.example.com/post/1001_1.jpg",
          "author": {
            "userId": 10002,
            "nickname": "美食达人",
            "avatar": "https://cdn.example.com/avatar/10002.jpg"
          },
          "likeCount": 128,
          "commentCount": 32
        },
        "createdAt": "2025-11-26 15:00:00"
      },
      {
        "favoriteId": 2002,
        "targetType": "skill",
        "targetId": 5001,
        "skill": {
          "skillId": 5001,
          "skillName": "王者荣耀陪玩",
          "coverImage": "https://cdn.example.com/skill/wzry.jpg",
          "price": 30.00,
          "priceUnit": "局",
          "provider": {
            "userId": 10003,
            "nickname": "游戏达人",
            "avatar": "https://cdn.example.com/avatar/10003.jpg"
          }
        },
        "createdAt": "2025-11-25 10:00:00"
      }
    ],
    "total": 100,
    "page": 1,
    "pageSize": 20,
    "hasMore": true
  }
}
```

**字段说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| favoriteId | long | 是 | 收藏记录ID |
| targetType | string | 是 | 收藏类型: `post`, `skill`, `activity`, `user` |
| targetId | long | 是 | 收藏目标ID |
| post | object | 条件 | 当 targetType=post 时返回 |
| skill | object | 条件 | 当 targetType=skill 时返回 |
| createdAt | string | 是 | 收藏时间 |

---

### 7. 获取点赞列表

**接口说明**: 点击"点赞"Tab 时调用

**请求**
```http
GET /xypai-user/api/user/profile/likes?page=1&pageSize=20
Authorization: Bearer <token>
```

**响应 JSON**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "likeId": 3001,
        "targetType": "post",
        "targetId": 1001,
        "post": {
          "postId": 1001,
          "content": "今天发现一家超好吃的火锅店！",
          "coverImage": "https://cdn.example.com/post/1001_1.jpg",
          "author": {
            "userId": 10002,
            "nickname": "美食达人",
            "avatar": "https://cdn.example.com/avatar/10002.jpg"
          },
          "likeCount": 128,
          "commentCount": 32
        },
        "createdAt": "2025-11-26 15:00:00"
      }
    ],
    "total": 200,
    "page": 1,
    "pageSize": 20,
    "hasMore": true
  }
}
```

---

### 8-16. 单字段更新接口

以下接口用于编辑资料页面的实时保存：

#### 8. 更新昵称

**请求**
```http
PUT /xypai-user/api/user/profile/nickname
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "nickname": "新昵称"
}
```

**校验规则**
- 长度: 2-20 字符
- 不能包含特殊字符: `@`, `#`, `$`, `%`, `&`, `*`
- 不能是纯数字
- 不能包含敏感词

**响应 JSON**
```json
{
  "code": 200,
  "msg": "更新成功",
  "data": null
}
```

**错误响应**
```json
{
  "code": 400,
  "msg": "昵称长度必须在2-20个字符之间",
  "data": null
}
```

---

#### 9. 更新性别

**请求**
```http
PUT /xypai-user/api/user/profile/gender
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "gender": "male"
}
```

**校验规则**
- 有效值: `male`, `female`, `other`

---

#### 10. 更新生日

**请求**
```http
PUT /xypai-user/api/user/profile/birthday
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "birthday": "1995-06-15"
}
```

**校验规则**
- 格式: YYYY-MM-DD
- 不能是未来日期
- 年龄必须 >= 18 岁

---

#### 11. 更新居住地

**请求**
```http
PUT /xypai-user/api/user/profile/residence
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "residence": "广东省广州市天河区"
}
```

---

#### 12. 更新身高

**请求**
```http
PUT /xypai-user/api/user/profile/height
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "height": 175
}
```

**校验规则**
- 范围: 100-250 cm

---

#### 13. 更新体重

**请求**
```http
PUT /xypai-user/api/user/profile/weight
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "weight": 65
}
```

**校验规则**
- 范围: 30-200 kg

---

#### 14. 更新职业

**请求**
```http
PUT /xypai-user/api/user/profile/occupation
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "occupation": "软件工程师"
}
```

---

#### 15. 更新微信号

**请求**
```http
PUT /xypai-user/api/user/profile/wechat
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "wechat": "wechat_id_123"
}
```

**校验规则**
- 长度: 6-20 字符
- 只能包含字母、数字、下划线、减号

---

#### 16. 更新个性签名

**请求**
```http
PUT /xypai-user/api/user/profile/bio
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "bio": "热爱生活，分享美好"
}
```

**校验规则**
- 最大长度: 200 字符
- 不能包含敏感词

---

### 17. 上传头像

**请求**
```http
POST /xypai-user/api/user/profile/avatar/upload
Content-Type: multipart/form-data
Authorization: Bearer <token>
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| avatar | file | 是 | 头像图片文件 |

**校验规则**
- 支持格式: jpg, jpeg, png, webp
- 最大文件大小: 5MB
- 建议尺寸: 400x400 以上

**响应 JSON**
```json
{
  "code": 200,
  "msg": "上传成功",
  "data": {
    "avatarUrl": "https://cdn.example.com/avatar/10001_new.jpg",
    "thumbnailUrl": "https://cdn.example.com/avatar/10001_new_thumb.jpg"
  }
}
```

---

## 🟡 优先级 P1 - 技能管理接口

### 1. 创建线上技能

**接口说明**: 创建游戏陪玩类技能

**请求**
```http
POST /xypai-user/api/user/skills/online
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "gameId": "wzry",
  "gameName": "王者荣耀",
  "gameRank": "王者",
  "skillName": "王者荣耀陪玩",
  "description": "王者50星巅峰赛选手，可带上分或娱乐陪玩，声音好听，性格温柔。",
  "price": 30.00,
  "serviceHours": 1,
  "coverImage": "https://cdn.example.com/skill/wzry.jpg",
  "images": [
    "https://cdn.example.com/skill/wzry_1.jpg",
    "https://cdn.example.com/skill/wzry_2.jpg"
  ],
  "promises": [
    "不骂人",
    "准时上线",
    "包上分"
  ],
  "isOnline": true
}
```

**字段说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| gameId | string | 否 | 游戏ID |
| gameName | string | 是 | 游戏名称 |
| gameRank | string | 是 | 游戏段位 |
| skillName | string | 是 | 技能名称，2-50字符 |
| description | string | 是 | 技能介绍，10-500字符 |
| price | decimal | 是 | 价格，必须>0 |
| serviceHours | decimal | 是 | 每局/每次服务时长（小时） |
| coverImage | string | 否 | 封面图URL |
| images | array | 否 | 技能图片URL列表，最多9张 |
| promises | array | 否 | 服务承诺列表，最多5个 |
| isOnline | boolean | 否 | 是否上架，默认false |

**响应 JSON**
```json
{
  "code": 200,
  "msg": "创建成功",
  "data": {
    "skillId": 5001
  }
}
```

---

### 2. 创建线下技能

**接口说明**: 创建本地服务类技能

**请求**
```http
POST /xypai-user/api/user/skills/offline
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "serviceType": "photography",
  "serviceTypeName": "摄影服务",
  "skillName": "专业人像摄影",
  "description": "专业摄影师，擅长人像、写真、商业摄影。提供修图服务，出片快。",
  "price": 200.00,
  "priceUnit": "次",
  "coverImage": "https://cdn.example.com/skill/photo.jpg",
  "images": [
    "https://cdn.example.com/skill/photo_1.jpg",
    "https://cdn.example.com/skill/photo_2.jpg"
  ],
  "location": {
    "address": "广东省深圳市南山区科技园",
    "latitude": 22.5431,
    "longitude": 114.0579
  },
  "availableTimes": [
    {
      "dayOfWeek": 6,
      "startTime": "09:00",
      "endTime": "18:00"
    },
    {
      "dayOfWeek": 7,
      "startTime": "09:00",
      "endTime": "18:00"
    }
  ],
  "promises": [
    "准时到达",
    "精修10张"
  ],
  "isOnline": true
}
```

**字段说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| serviceType | string | 是 | 服务类型ID |
| serviceTypeName | string | 是 | 服务类型名称 |
| skillName | string | 是 | 技能名称，2-50字符 |
| description | string | 是 | 技能介绍，10-500字符 |
| price | decimal | 是 | 价格，必须>0 |
| priceUnit | string | 是 | 价格单位: `次`, `小时`, `天` |
| location | object | 是 | 服务地点 |
| location.address | string | 是 | 详细地址 |
| location.latitude | decimal | 是 | 纬度 |
| location.longitude | decimal | 是 | 经度 |
| availableTimes | array | 是 | 可用时间段，至少1个 |

---

### 3. 获取我的技能列表

**请求**
```http
GET /xypai-user/api/user/skills/my?pageNum=1&pageSize=10&skillType=online
Authorization: Bearer <token>
```

**查询参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| pageNum | integer | 否 | 1 | 页码 |
| pageSize | integer | 否 | 10 | 每页数量 |
| skillType | string | 否 | - | 技能类型: `online`, `offline` |

**响应 JSON (TableDataInfo格式)**
```json
{
  "total": 3,
  "rows": [
    {
      "skillId": 5001,
      "skillName": "王者荣耀陪玩",
      "skillType": "online",
      "coverImage": "https://cdn.example.com/skill/wzry.jpg",
      "price": 30.00,
      "priceUnit": "局",
      "isOnline": true,
      "rating": 4.8,
      "reviewCount": 128,
      "orderCount": 200,
      "gameName": "王者荣耀",
      "gameRank": "王者",
      "createdAt": "2025-11-01 10:00:00",
      "updatedAt": "2025-11-27 15:30:00"
    }
  ],
  "code": 200,
  "msg": "查询成功"
}
```

---

### 4. 获取技能详情

**请求**
```http
GET /xypai-user/api/user/skills/{skillId}
Authorization: Bearer <token> (可选)
```

**响应 JSON**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "skillId": 5001,
    "userId": 10001,
    "skillName": "王者荣耀陪玩",
    "skillType": "online",
    "gameName": "王者荣耀",
    "gameRank": "王者",
    "description": "王者50星巅峰赛选手，可带上分或娱乐陪玩",
    "price": 30.00,
    "priceUnit": "局",
    "serviceHours": 1,
    "coverImage": "https://cdn.example.com/skill/wzry.jpg",
    "images": [
      "https://cdn.example.com/skill/wzry_1.jpg"
    ],
    "promises": [
      "不骂人",
      "准时上线"
    ],
    "isOnline": true,
    "rating": 4.8,
    "reviewCount": 128,
    "orderCount": 200,
    "userInfo": {
      "userId": 10001,
      "nickname": "游戏达人",
      "avatar": "https://cdn.example.com/avatar/10001.jpg",
      "isOnline": true,
      "isVerified": true
    },
    "reviews": {
      "summary": {
        "averageRating": 4.8,
        "totalCount": 128,
        "ratingDistribution": {
          "5": 100,
          "4": 20,
          "3": 5,
          "2": 2,
          "1": 1
        }
      },
      "recentReviews": [
        {
          "reviewId": 8001,
          "userId": 10002,
          "nickname": "玩家A",
          "avatar": "https://cdn.example.com/avatar/10002.jpg",
          "rating": 5,
          "content": "技术很好，带我上分了！",
          "createdAt": "2025-11-26 20:00:00"
        }
      ]
    },
    "createdAt": "2025-11-01 10:00:00"
  }
}
```

---

### 5. 更新技能

**请求**
```http
PUT /xypai-user/api/user/skills/{skillId}
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体**
```json
{
  "skillName": "王者荣耀陪玩-新赛季",
  "description": "更新后的描述",
  "price": 35.00,
  "promises": ["不骂人", "准时上线", "新赛季更强"]
}
```

**响应 JSON**
```json
{
  "code": 200,
  "msg": "更新成功",
  "data": null
}
```

---

### 6. 删除技能

**请求**
```http
DELETE /xypai-user/api/user/skills/{skillId}
Authorization: Bearer <token>
```

**响应 JSON**
```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null
}
```

---

### 7. 切换技能上下架状态

**请求**
```http
PUT /xypai-user/api/user/skills/{skillId}/toggle?isOnline=true
Authorization: Bearer <token>
```

**响应 JSON**
```json
{
  "code": 200,
  "msg": "已上架",
  "data": null
}
```

---

### 8. 获取用户技能列表

**接口说明**: 获取指定用户的公开技能列表

**请求**
```http
GET /xypai-user/api/user/skills/user/{userId}?pageNum=1&pageSize=20
Authorization: Bearer <token> (可选)
```

**响应 JSON (TableDataInfo格式)**
```json
{
  "total": 5,
  "rows": [
    {
      "skillId": 5001,
      "skillName": "王者荣耀陪玩",
      "skillType": "online",
      "coverImage": "https://cdn.example.com/skill/wzry.jpg",
      "price": 30.00,
      "priceUnit": "局",
      "rating": 4.8,
      "reviewCount": 128,
      "orderCount": 200,
      "gameName": "王者荣耀",
      "gameRank": "王者"
    }
  ],
  "code": 200,
  "msg": "查询成功"
}
```

---

### 9. 搜索附近技能

**请求**
```http
GET /xypai-user/api/user/skills/nearby?latitude=22.5431&longitude=114.0579&radiusMeters=10000&pageNum=1&pageSize=10
Authorization: Bearer <token> (可选)
```

**查询参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| latitude | decimal | 是 | - | 用户纬度 |
| longitude | decimal | 是 | - | 用户经度 |
| radiusMeters | integer | 否 | 10000 | 搜索半径（米） |
| serviceType | string | 否 | - | 服务类型过滤 |
| pageNum | integer | 否 | 1 | 页码 |
| pageSize | integer | 否 | 10 | 每页数量 |

**响应 JSON (TableDataInfo格式)**
```json
{
  "total": 20,
  "rows": [
    {
      "skillId": 5002,
      "skillName": "专业人像摄影",
      "skillType": "offline",
      "coverImage": "https://cdn.example.com/skill/photo.jpg",
      "price": 200.00,
      "priceUnit": "次",
      "rating": 5.0,
      "reviewCount": 50,
      "serviceType": "photography",
      "serviceLocation": "深圳市南山区",
      "distance": 1.5,
      "userInfo": {
        "userId": 10002,
        "nickname": "摄影师小王",
        "avatar": "https://cdn.example.com/avatar/10002.jpg",
        "isOnline": true
      }
    }
  ],
  "code": 200,
  "msg": "查询成功"
}
```

---

## 🟢 已完成 - 社交关系接口

以下接口已在 `relationApi.ts` 中实现，后端接口已就绪：

| 接口 | 方法 | 路径 | 状态 |
|------|------|------|------|
| 关注用户 | POST | `/relation/follow/{id}` | ✅ |
| 取消关注 | DELETE | `/relation/follow/{id}` | ✅ |
| 获取粉丝列表 | GET | `/relation/fans` | ✅ |
| 获取关注列表 | GET | `/relation/following` | ✅ |
| 拉黑用户 | POST | `/relation/block/{id}` | ✅ |
| 取消拉黑 | DELETE | `/relation/block/{id}` | ✅ |
| 举报用户 | POST | `/relation/report/{id}` | ✅ |

---

## 📋 统一错误码

| 错误码 | 说明 | 示例场景 |
|--------|------|----------|
| 200 | 操作成功 | - |
| 400 | 请求参数错误 | 昵称长度不符合要求 |
| 401 | 未授权 | Token 无效或过期 |
| 403 | 无权限 | 操作他人的资源 |
| 404 | 资源不存在 | 用户/技能不存在 |
| 409 | 资源冲突 | 昵称已被占用 |
| 422 | 业务校验失败 | 年龄不满18岁 |
| 429 | 请求过于频繁 | 短时间内多次请求 |
| 500 | 服务器内部错误 | 数据库异常 |

---

## 🧪 测试用例参考

参考现有测试文件：
- `AppProfilePageTest.java` - 个人主页测试
- `AppOtherUserProfilePageTest.java` - 他人主页测试
- `AppEditProfilePageTest.java` - 编辑资料测试

---

## 📌 实现优先级

### 第一批 (P0) - 核心页面接口 ✅ 已验证完成
1. ✅ `/profile/edit` 接口 - 已实现
2. ✅ `/profile/header` 接口 - 已实现
3. ✅ `/profile/other/{userId}` 接口 - 已实现
4. ⚠️ `/profile/posts` 接口 - **Stub，需要 xypai-content RPC 集成**
5. ⚠️ `/profile/favorites` 接口 - **Stub，需要 xypai-content RPC 集成**
6. ⚠️ `/profile/likes` 接口 - **Stub，需要 xypai-content RPC 集成**
7. ✅ `/profile/info` 接口 - 已实现（包含技能列表）

### 第二批 (P1) - 技能管理接口 ✅ 已验证完成
1. ✅ `/skills/online` 创建接口 - 已实现
2. ✅ `/skills/offline` 创建接口 - 已实现
3. ✅ `/skills/my` 列表接口 - 已实现
4. ✅ `/skills/{skillId}` 详情接口 - 已实现
5. ✅ `/skills/user/{userId}` 用户技能接口 - 已实现
6. ✅ `/skills/nearby` 附近技能接口 - 已实现
7. ✅ `/skills/{skillId}` 更新接口 - 已实现
8. ✅ `/skills/{skillId}` 删除接口 - 已实现
9. ✅ `/skills/{skillId}/toggle` 上下架接口 - 已实现

### 第三批 (P2) - 增强功能
1. ⚠️ 头像上传接口 - **需要集成 OSS**
2. 🔲 背景图上传接口 - 未实现
3. 🔲 隐私设置接口 - 未实现

---

## 🔧 前端接口调用指南

### 个人主页 Tab 切换接口对照表

| Tab | 接口 | 服务 | 认证 |
|-----|------|------|------|
| **资料** | `GET /xypai-user/api/user/profile/info` | xypai-user | Bearer Token |
| **动态** | `GET /xypai-content/api/v1/content/feed/user/{userId}` | xypai-content | Bearer Token |
| **收藏** | `GET /xypai-content/api/v1/interaction/collect/my` | xypai-content | Bearer Token |
| **点赞** | `GET /xypai-content/api/v1/interaction/like/my?type=feed` | xypai-content | Bearer Token |

### 动态列表接口详情

**请求**
```http
GET /xypai-content/api/v1/content/feed/user/{userId}?pageNum=1&pageSize=20
Authorization: Bearer <token>
```

**响应示例**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "records": [
      {
        "id": 1001,
        "userId": 10001,
        "content": "今天发现一家超好吃的火锅店！",
        "mediaList": [
          {
            "mediaType": "image",
            "url": "https://cdn.example.com/feed/1001.jpg"
          }
        ],
        "likeCount": 128,
        "commentCount": 32,
        "isLiked": false,
        "createdAt": "2025-11-27 10:30:00"
      }
    ],
    "total": 50,
    "current": 1,
    "pages": 3
  }
}
```

### 收藏列表接口详情

**请求**
```http
GET /xypai-content/api/v1/interaction/collect/my?pageNum=1&pageSize=20
Authorization: Bearer <token>
```

**响应示例**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "records": [
      {
        "id": 2001,
        "targetType": "feed",
        "targetId": 1001,
        "targetContent": "今天发现一家超好吃的火锅店！",
        "targetCover": "https://cdn.example.com/feed/1001_thumb.jpg",
        "author": {
          "userId": 10002,
          "nickname": "美食达人",
          "avatar": "https://cdn.example.com/avatar/10002.jpg"
        },
        "collectTime": "2025-11-26 15:00:00"
      }
    ],
    "total": 100,
    "current": 1,
    "pages": 5
  }
}
```

### 点赞列表接口详情

**请求**
```http
GET /xypai-content/api/v1/interaction/like/my?type=feed&pageNum=1&pageSize=20
Authorization: Bearer <token>
```

**查询参数**
| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| type | string | 否 | feed | 点赞类型: `feed`(动态), `comment`(评论), `all`(全部) |
| pageNum | integer | 否 | 1 | 页码 |
| pageSize | integer | 否 | 20 | 每页数量 |

**响应示例**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "records": [
      {
        "id": 3001,
        "targetType": "feed",
        "targetId": 1001,
        "targetContent": "今天发现一家超好吃的火锅店！",
        "targetCover": "https://cdn.example.com/feed/1001_thumb.jpg",
        "author": {
          "userId": 10002,
          "nickname": "美食达人",
          "avatar": "https://cdn.example.com/avatar/10002.jpg"
        },
        "likeTime": "2025-11-26 15:00:00"
      }
    ],
    "total": 200,
    "current": 1,
    "pages": 10
  }
}
```

---

**文档版本**: v1.0.0

**最后更新**: 2025-11-28

**负责人**: XyPai Backend Team
