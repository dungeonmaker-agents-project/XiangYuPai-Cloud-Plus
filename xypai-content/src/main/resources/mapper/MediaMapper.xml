<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xypai.content.mapper.MediaMapper">

    <!-- ===== 查询关联对象的媒体列表 ===== -->
    <select id="selectByRef" resultType="com.xypai.content.domain.entity.Media">
        SELECT *
        FROM media
        WHERE 
            ref_type = #{refType}
            AND ref_id = #{refId}
            AND status = 1
        ORDER BY created_at ASC
    </select>

    <!-- ===== 批量查询关联媒体 ===== -->
    <select id="selectBatchByRef" resultType="com.xypai.content.domain.entity.Media">
        SELECT *
        FROM media
        WHERE 
            ref_type = #{refType}
            AND ref_id IN
            <foreach collection="refIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
            AND status = 1
        ORDER BY ref_id, created_at ASC
    </select>

    <!-- ===== 删除关联媒体 ===== -->
    <delete id="deleteByRef">
        DELETE FROM media
        WHERE 
            ref_type = #{refType}
            AND ref_id = #{refId}
    </delete>

    <!-- ===== 统计用户上传媒体数量 ===== -->
    <select id="countByUploaderId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM media
        WHERE 
            uploader_id = #{uploaderId}
            AND status = 1
    </select>

</mapper>

