<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xypai.content.mapper.CommentLikeMapper">

    <!-- ===== 查询用户是否已点赞评论 ===== -->
    <select id="selectByCommentIdAndUserId" resultType="com.xypai.content.domain.entity.CommentLike">
        SELECT *
        FROM comment_like
        WHERE 
            comment_id = #{commentId}
            AND user_id = #{userId}
        LIMIT 1
    </select>

    <!-- ===== 批量查询用户点赞状态 ===== -->
    <select id="selectBatchByCommentIdsAndUserId" resultType="com.xypai.content.domain.entity.CommentLike">
        SELECT *
        FROM comment_like
        WHERE 
            comment_id IN
            <foreach collection="commentIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
            AND user_id = #{userId}
            AND status = 1
    </select>

</mapper>

