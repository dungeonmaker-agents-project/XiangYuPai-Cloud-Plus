# 前端页面测试映射规范

> **更新时间**: 2025-11-24
> **维护者**: XyPai 开发团队

## 📋 概述

本文档定义了前端页面与后端测试文件的映射规范，确保每个前端页面都有对应的集成测试，并且能够快速定位测试文件和接口归属。

---

## 🎯 核心原则

### 1. **一个前端页面 = 一个测试类**

每个前端页面文档对应一个独立的测试类，测试该页面所需的所有接口。

### 2. **测试文件命名规范**

```
{Page}{Feature}Test.java

示例:
- HomePage Feed流 → AppHomeFeedTest.java
- 发布动态 → ContentPublishTest.java
- 筛选页面 → HomeFilterTest.java
- 动态详情 → FeedDetailTest.java
```

### 3. **测试文件标签（Javadoc）**

每个测试类必须包含标准化的 Javadoc 标签，用于追溯：

```java
/**
 * {页面名称}集成测试
 *
 * 🎯 核心目标:
 * 测试{页面功能描述}
 *
 * 📌 前端页面映射:
 * - 文档路径: XiangYuPai-Doc/Action-API/模块化架构/{模块}/Frontend/{页面文档}.md
 * - 页面路由: {前端路由}
 * - 页面类型: {页面类型}
 *
 * 📌 测试接口归属说明:
 * - {接口1} → {归属模块} ({服务说明})
 * - {接口2} → {归属模块} ({服务说明})
 *
 * 测试流程:
 * 1. {测试步骤1}
 * 2. {测试步骤2}
 * ...
 *
 * 💡 测试方式说明:
 * - 集成测试，调用真实服务
 * - 需要启动: {依赖服务列表}
 *
 * @author XyPai Team
 * @date {创建日期}
 * @frontend-page {前端页面文档名称}
 * @module {归属模块}
 */
```

### 4. **测试文件位置**

测试文件放在对应的微服务模块的 `src/test/java` 目录：

```
xypai-aggregation/xypai-app-bff/src/test/java/org/dromara/aggregation/
  ├── AppHomeFeedTest.java          # 01-首页Feed流页面
  └── HomeFilterTest.java           # 02-筛选页面

xypai-modules/xypai-content/src/test/java/org/dromara/content/
  ├── ContentPublishTest.java       # 02-发布动态页面
  ├── FeedDetailTest.java           # 03-动态详情页面
  ├── DiscoveryFeedTest.java        # 01-发现主页页面
  ├── SearchTest.java               # 06-搜索页面 + 07-搜索结果页面
  ├── ActivityTest.java             # 08-组局中心 + 09-组局详情 + 10-发布组局
  └── ServiceReviewTest.java        # 12-服务详情(评价部分)

xypai-modules/xypai-user/src/test/java/org/dromara/user/
  ├── UserFollowTest.java           # 关注功能
  ├── UserSearchTest.java           # 用户搜索
  └── ServiceTest.java              # 11-服务列表 + 12-服务详情

xypai-common/src/test/java/org/dromara/common/
  ├── MediaUploadTest.java          # 媒体上传
  └── LocationTest.java             # 位置服务
```

---

## 📄 前端页面与测试文件映射表

### Content 模块 (03-content模块/Frontend)

| # | 前端页面文档 | 页面路由 | 测试文件 | 归属模块 | 实现状态 |
|---|------------|---------|---------|---------|---------|
| 01 | 首页Feed流页面 | `/home` | `AppHomeFeedTest.java` | xypai-app-bff | ✅ 已完成 |
| 02a | 筛选页面 | `/home/filter` | `HomeFilterTest.java` | xypai-app-bff | 🔜 待创建 |
| 02b | 发布动态页面 | `/publish` | `ContentPublishTest.java` | xypai-content | ✅ 已完成 |
| 03 | 动态详情页面 | `/feed/:id` | `FeedDetailTest.java` | xypai-content | 🔜 待创建 |
| 01b | 发现主页页面 | `/discovery` | `DiscoveryFeedTest.java` | xypai-content | 🔜 待创建 |
| 05 | 限时专享页面 | `/flash-deals` | `FlashDealTest.java` | xypai-content/trade | 🔜 待创建 |
| 06+07 | 搜索页面+搜索结果 | `/search` | `SearchTest.java` | xypai-content + xypai-user | 🔜 待创建 |
| 08+09+10 | 组局中心+详情+发布 | `/activities` | `ActivityTest.java` | xypai-content | 🔜 待创建 |
| 11+12 | 服务列表+详情 | `/services` | `ServiceTest.java` | xypai-user + xypai-content | 🔜 待创建 |

---

## 🏗️ 测试文件标准模板

### 模板1: BFF 聚合服务测试

```java
package org.dromara.aggregation;

import com.fasterxml.jackson.databind.ObjectMapper;
import lombok.extern.slf4j.Slf4j;
import org.junit.jupiter.api.*;
import org.springframework.http.*;
import org.springframework.web.client.RestTemplate;

import java.util.*;

/**
 * {页面名称}集成测试 (BFF 聚合服务)
 *
 * 🎯 核心目标:
 * 测试{页面功能描述}
 *
 * 📌 前端页面映射:
 * - 文档路径: XiangYuPai-Doc/Action-API/模块化架构/03-content模块/Frontend/{XX-页面名称}.md
 * - 页面路由: {前端路由}
 * - 页面类型: {Tab页面/全屏页面/弹窗页面}
 *
 * 📌 测试接口归属说明:
 * - {接口1} → xypai-app-bff (聚合层，无数据库)
 *   该接口聚合 xypai-user + xypai-content 数据
 *
 * 测试流程:
 * 1. 📱 用户登录 (调用 xypai-auth)
 * 2. 🔍 {测试场景1}
 * 3. ✅ {测试场景2}
 * ...
 *
 * 💡 测试方式说明:
 * - 集成测试，调用真实服务
 * - 需要启动: Gateway(8080), xypai-auth(8200), xypai-app-bff(9400), Nacos, Redis
 *
 * @author XyPai Team
 * @date {创建日期}
 * @frontend-page {XX-页面名称}.md
 * @module xypai-app-bff
 */
@Slf4j
@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
public class {ClassName}Test {

    // 测试配置
    private static final String GATEWAY_URL = "http://localhost:8080";
    private static final String TEST_COUNTRY_CODE = "+86";
    private static final String TEST_SMS_CODE = "123456";

    // HTTP 客户端
    private static RestTemplate restTemplate;
    private static ObjectMapper objectMapper;

    // 保存登录后的 Token
    private static String authToken;
    private static String userId;

    @BeforeAll
    static void setup() {
        restTemplate = new RestTemplate();
        objectMapper = new ObjectMapper();
        log.info("🧪 {页面名称}集成测试启动 (BFF 聚合服务)");
        log.info("⚠️ 确保服务已启动: Gateway(8080), xypai-auth(8200), xypai-app-bff(9400), Nacos, Redis\n");
    }

    /**
     * 辅助方法: 确保有有效的登录 Token
     */
    private static void ensureAuthenticated() {
        // ... (标准登录逻辑)
    }

    /**
     * 🎯 测试1: 用户登录
     */
    @Test
    @Order(1)
    @DisplayName("测试1: 用户登录")
    public void test1_UserLogin() {
        // ... (标准登录测试)
    }

    /**
     * 🎯 测试2: {核心功能1}
     *
     * 接口: {HTTP方法} {接口路径}
     * 服务: xypai-app-bff (聚合层)
     */
    @Test
    @Order(2)
    @DisplayName("测试2: {核心功能1}")
    public void test2_{FunctionName}() {
        // 测试逻辑
    }

    @AfterAll
    static void tearDown() {
        log.info("\n🎉 {页面名称}测试完成！");
    }
}
```

### 模板2: 领域服务测试

```java
package org.dromara.content;  // 或 org.dromara.user

/**
 * {页面名称}集成测试
 *
 * 🎯 核心目标:
 * 测试{页面功能描述}
 *
 * 📌 前端页面映射:
 * - 文档路径: XiangYuPai-Doc/Action-API/模块化架构/03-content模块/Frontend/{XX-页面名称}.md
 * - 页面路由: {前端路由}
 * - 页面类型: {页面类型}
 *
 * 📌 测试接口归属说明:
 * - {接口1} → xypai-content (内容服务)
 *   {接口说明}
 *
 * 测试流程:
 * 1. 📱 用户登录 (调用 xypai-auth)
 * 2. 📝 {测试场景1}
 * 3. ✅ {测试场景2}
 * ...
 *
 * 💡 测试方式说明:
 * - 集成测试，调用真实服务
 * - 需要启动: Gateway(8080), xypai-auth(8200), xypai-content(9403), Nacos, Redis, MySQL
 *
 * @author XyPai Team
 * @date {创建日期}
 * @frontend-page {XX-页面名称}.md
 * @module xypai-content
 */
@Slf4j
@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
public class {ClassName}Test {
    // 类似 BFF 测试模板，但服务 URL 不同
}
```

---

## 🔍 快速查找测试文件

### 方法1: 通过前端页面文档查找

```bash
# 在测试文件中搜索 @frontend-page 标签
grep -r "@frontend-page 02-发布动态页面.md" xypai-*/src/test/
```

输出:
```
xypai-modules/xypai-content/src/test/java/org/dromara/content/ContentPublishTest.java
```

### 方法2: 通过接口路径查找

```bash
# 搜索测试文件中的接口路径
grep -r "POST /api/v1/content/publish" xypai-*/src/test/
```

### 方法3: 通过页面路由查找

```bash
# 搜索 Javadoc 中的页面路由
grep -r "页面路由: /publish" xypai-*/src/test/
```

### 方法4: 使用 IDE 全局搜索

在 IDEA 中:
1. `Ctrl+Shift+F` 打开全局搜索
2. 搜索: `@frontend-page {页面文档名}`
3. 范围: `Tests`

---

## 📊 测试覆盖率跟踪

### 已完成测试

| 前端页面 | 测试文件 | 测试用例数 | 覆盖接口 | 状态 |
|---------|---------|-----------|---------|------|
| 01-首页Feed流页面 | `AppHomeFeedTest.java` | 5 | 1/2 | ⚠️ 缺少关注接口测试 |
| 02-发布动态页面 | `ContentPublishTest.java` | 10 | 3/3 | ✅ 完整 |

### 待创建测试 (按优先级)

#### 🔥 高优先级

| 前端页面 | 测试文件 | 归属模块 | 预计测试用例 | 依赖服务 |
|---------|---------|---------|------------|---------|
| 02-筛选页面 | `HomeFilterTest.java` | xypai-app-bff | 6 | Gateway, Auth, BFF, User |
| 01-发现主页页面 | `DiscoveryFeedTest.java` | xypai-content | 5 | Gateway, Auth, Content |
| 03-动态详情页面 | `FeedDetailTest.java` | xypai-content | 8 | Gateway, Auth, Content |
| 06+07-搜索页面 | `SearchTest.java` | xypai-content | 7 | Gateway, Auth, Content |

#### 📦 中优先级

| 前端页面 | 测试文件 | 归属模块 | 预计测试用例 | 依赖服务 |
|---------|---------|---------|------------|---------|
| 08+09+10-组局功能 | `ActivityTest.java` | xypai-content | 8 | Gateway, Auth, Content |
| 11+12-服务功能 | `ServiceTest.java` | xypai-user | 6 | Gateway, Auth, User, Content |

#### 📦 低优先级

| 前端页面 | 测试文件 | 归属模块 | 预计测试用例 | 依赖服务 |
|---------|---------|---------|------------|---------|
| 05-限时专享 | `FlashDealTest.java` | xypai-content/trade | 4 | Gateway, Auth, Content/Trade |

---

## 🚀 测试创建流程

### Step 1: 阅读前端页面文档

```bash
# 打开前端页面文档
code "E:/Users/Administrator/Documents/GitHub/XiangYuPai-Doc/Action-API/模块化架构/03-content模块/Frontend/02-筛选页面.md"
```

### Step 2: 确定接口归属

参考 [Content模块接口归属分析.md](./Content模块接口归属分析.md)，确定接口应该在哪个模块实现。

### Step 3: 创建测试文件

```bash
# 在对应模块创建测试文件
# 例如: xypai-app-bff
cd xypai-aggregation/xypai-app-bff/src/test/java/org/dromara/aggregation/
touch HomeFilterTest.java
```

### Step 4: 使用模板编写测试

1. 复制对应的测试模板
2. 替换占位符 `{ClassName}`, `{页面名称}` 等
3. 填写 Javadoc 标签: `@frontend-page`, `@module`
4. 编写测试用例

### Step 5: 运行测试

```bash
# 单个测试
mvn test -Dtest=HomeFilterTest

# 模块所有测试
cd xypai-aggregation/xypai-app-bff
mvn test
```

### Step 6: 更新映射表

在本文档中更新 [前端页面与测试文件映射表](#前端页面与测试文件映射表)，标记测试状态为 ✅。

---

## 📝 测试文件命名冲突处理

如果多个前端页面对应同一个后端功能，合并测试文件：

```java
/**
 * 搜索功能集成测试
 *
 * 📌 前端页面映射:
 * - 文档路径: XiangYuPai-Doc/Action-API/模块化架构/03-content模块/Frontend/06-搜索页面.md
 * - 文档路径: XiangYuPai-Doc/Action-API/模块化架构/03-content模块/Frontend/07-搜索结果页面.md
 * - 页面路由: /search
 *
 * @frontend-page 06-搜索页面.md, 07-搜索结果页面.md
 * @module xypai-content
 */
@Slf4j
public class SearchTest {
    // 合并两个页面的测试用例
}
```

---

## 🔗 相关文档

- [Content模块接口归属分析](./Content模块接口归属分析.md) - 接口归属详细分析
- [xypai-app-bff 实现进度](./xypai-app-bff/实现进度.md) - BFF 层实现进度
- [xypai-content 实现进度](../xypai-modules/xypai-content/实现进度.md) - Content 服务实现进度
- [向娱拍平台架构总览](../向娱拍平台架构总览.md) - 整体架构

---

## 📌 重要提示

1. **每个前端页面都必须有对应的测试文件**，确保接口稳定性
2. **测试文件必须包含 `@frontend-page` 标签**，方便追溯
3. **测试文件名必须遵循命名规范**，便于快速定位
4. **测试用例必须覆盖页面的所有核心功能**，包括正常流程和异常验证
5. **测试必须是集成测试**，调用真实的微服务，不使用 Mock

---

**最后更新**: 2025-11-24
**更新内容**: 创建前端页面测试映射规范，定义标准化测试文件结构
