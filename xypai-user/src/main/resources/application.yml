# Tomcat
server:
  port: 9401

# Spring
spring:
  application:
    # 应用名称
    name: xypai-user
  profiles:
    # 环境配置
    active: @profiles.active@

# ==========================================
# 🔧 租户配置 - xypai-user 模块特殊配置
# ==========================================
# 说明: xypai-user 的 user 表不使用租户隔离
# 原因: user 表与 sys_user 关联，无需按租户隔离
# ==========================================
tenant:
  # 启用租户功能（框架级别）
  enable: true
  # 排除不需要租户隔离的表
  excludes:
    - user                          # 用户核心信息表
    - user_relation                 # 用户关系表
    - user_stats                    # 用户统计数据表
    - user_wallet                   # 用户钱包表
    - user_transaction              # 用户交易记录表
    - user_occupation               # 用户职业/技能表
    - user_dict                     # 用户字典配置表

--- # nacos 配置
spring:
  cloud:
    nacos:
      # nacos 服务地址
      server-addr: @nacos.server@
      username: @nacos.username@
      password: @nacos.password@
      discovery:
        # 注册组
        group: @nacos.discovery.group@
        namespace: ${spring.profiles.active}
      config:
        # 配置组
        group: @nacos.config.group@
        namespace: ${spring.profiles.active}
  config:
    import:
      - optional:nacos:application-common.yml
      - optional:nacos:datasource.yml
      - optional:nacos:${spring.application.name}.yml