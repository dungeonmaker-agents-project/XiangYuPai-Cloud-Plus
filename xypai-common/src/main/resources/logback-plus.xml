<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 导入通用配置 -->
    <include resource="logback-plus.xml"/>

    <!-- 日志文件名称 -->
    <property name="log.name" value="xypai-common"/>

    <!-- 日志输出路径 -->
    <property name="log.path" value="/var/logs/${log.name}"/>

    <!-- 控制台输出 -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 系统日志文件 -->
    <appender name="file_info" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/info.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path}/info.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- 错误日志文件 -->
    <appender name="file_error" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path}/error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- 系统模块日志级别 -->
    <logger name="org.dromara.common" level="info"/>

    <!-- 业务模块日志级别 -->
    <logger name="org.dromara.common.location" level="debug"/>
    <logger name="org.dromara.common.media" level="debug"/>
    <logger name="org.dromara.common.notification" level="debug"/>
    <logger name="org.dromara.common.report" level="debug"/>

    <!-- Spring日志级别 -->
    <logger name="org.springframework" level="warn"/>

    <!-- MyBatis日志级别 -->
    <logger name="com.baomidou.mybatisplus" level="info"/>

    <!-- Dubbo日志级别 -->
    <logger name="org.apache.dubbo" level="warn"/>

    <!-- Root日志级别 -->
    <root level="info">
        <appender-ref ref="console"/>
        <appender-ref ref="file_info"/>
        <appender-ref ref="file_error"/>
    </root>
</configuration>
