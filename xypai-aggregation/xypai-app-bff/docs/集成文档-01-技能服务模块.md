# 集成文档 01: 技能服务模块 (Page11-12)

> 版本: v1.0.0 | 创建日期: 2025-11-26 | 优先级: ⭐⭐⭐⭐⭐ (最高)

---

## 📋 模块概述

### 业务描述
技能服务模块是向娱拍平台的核心业务之一，展示服务提供者（陪练/教练）的技能服务列表和详情，支持用户浏览、筛选、查看评价，最终下单购买服务。

### 对应前端页面
| 页面 | 路由 | 功能 |
|------|------|------|
| Page11 | `/service/list` | 服务列表（Tab切换/排序/筛选） |
| Page12 | `/service/detail` | 服务详情（提供者信息/评价） |

### 当前状态
- **BFF 层**: ✅ Mock 实现完成
- **RPC 接口**: ⏳ 待定义
- **领域服务**: ⏳ 待实现

---

## 🎯 集成目标

将 `SkillServiceServiceImpl` 中的 Mock 数据替换为真实的 Dubbo RPC 调用，从 `xypai-user` 服务获取技能服务数据。

---

## 📁 涉及文件清单

### BFF 层 (xypai-app-bff)

| 文件路径 | 类型 | 说明 | 集成时修改 |
|----------|------|------|-----------|
| `controller/SkillServiceController.java` | Controller | REST 接口 | ❌ 无需修改 |
| `service/SkillServiceService.java` | Interface | 服务接口 | ❌ 无需修改 |
| `service/impl/SkillServiceServiceImpl.java` | Impl | 服务实现 | ✅ **核心修改** |
| `domain/dto/ServiceListQueryDTO.java` | DTO | 列表查询请求 | ❌ 无需修改 |
| `domain/dto/ServiceReviewQueryDTO.java` | DTO | 评价查询请求 | ❌ 无需修改 |
| `domain/vo/ServiceCardVO.java` | VO | 服务卡片 | ❌ 无需修改 |
| `domain/vo/ServiceListResultVO.java` | VO | 列表结果 | ❌ 无需修改 |
| `domain/vo/ServiceDetailVO.java` | VO | 服务详情 | ❌ 无需修改 |
| `domain/vo/ServiceReviewListVO.java` | VO | 评价列表 | ❌ 无需修改 |

### RPC API 层 (ruoyi-api/xypai-api-appuser)

| 文件路径 | 类型 | 说明 | 操作 |
|----------|------|------|------|
| `RemoteAppUserService.java` | Interface | RPC 接口 | ✅ **新增3个方法** |
| `domain/dto/SkillServiceQueryDto.java` | DTO | 查询请求 | ✅ **新建** |
| `domain/vo/SkillServiceVo.java` | VO | 服务信息 | ✅ **新建** |
| `domain/vo/SkillServiceDetailVo.java` | VO | 服务详情 | ✅ **新建** |
| `domain/vo/SkillServicePageResult.java` | VO | 分页结果 | ✅ **新建** |
| `domain/vo/SkillServiceReviewVo.java` | VO | 评价信息 | ✅ **新建** |
| `domain/vo/SkillServiceReviewPageResult.java` | VO | 评价分页 | ✅ **新建** |

### 领域服务层 (xypai-modules/xypai-user)

| 文件路径 | 类型 | 说明 | 操作 |
|----------|------|------|------|
| `service/impl/RemoteAppUserServiceImpl.java` | Impl | RPC 实现 | ✅ **新增3个方法实现** |
| `service/SkillQueryService.java` | Service | 技能查询服务 | ✅ **新建（可选）** |
| `mapper/SkillMapper.java` | Mapper | 技能数据访问 | ✅ **新增查询方法** |

---

## 🔧 Step 1: RPC 接口定义

### 1.1 新增 DTO 类

**文件**: `ruoyi-api/xypai-api-appuser/src/main/java/org/dromara/appuser/api/domain/dto/SkillServiceQueryDto.java`

```java
package org.dromara.appuser.api.domain.dto;

import lombok.Data;
import java.io.Serial;
import java.io.Serializable;
import java.util.List;

/**
 * 技能服务查询 DTO
 */
@Data
public class SkillServiceQueryDto implements Serializable {

    @Serial
    private static final long serialVersionUID = 1L;

    /** 技能类型 (必填) */
    private String skillType;

    /** 页码 */
    private Integer pageNum = 1;

    /** 每页数量 */
    private Integer pageSize = 10;

    /** Tab类型: glory_king, online, offline, my */
    private String tabType;

    /** 排序: smart, price_asc, rating_desc, orders_desc */
    private String sortBy;

    /** 性别筛选: all, male, female */
    private String gender;

    /** 在线状态: online, active_3d, active_7d */
    private String status;

    /** 游戏大区 */
    private String gameArea;

    /** 段位列表 */
    private List<String> ranks;

    /** 价格区间 */
    private List<String> priceRanges;

    /** 位置/英雄 */
    private List<String> positions;

    /** 标签 */
    private List<String> tags;

    /** 城市代码 */
    private String cityCode;

    /** 当前用户ID (用于计算距离等) */
    private Long currentUserId;

    /** 当前用户经度 */
    private Double longitude;

    /** 当前用户纬度 */
    private Double latitude;
}
```

### 1.2 新增 VO 类

**文件**: `ruoyi-api/xypai-api-appuser/src/main/java/org/dromara/appuser/api/domain/vo/SkillServiceVo.java`

```java
package org.dromara.appuser.api.domain.vo;

import lombok.Data;
import java.io.Serial;
import java.io.Serializable;
import java.math.BigDecimal;
import java.util.List;

/**
 * 技能服务 VO (列表用)
 */
@Data
public class SkillServiceVo implements Serializable {

    @Serial
    private static final long serialVersionUID = 1L;

    /** 技能ID (即 serviceId) */
    private Long skillId;

    // ===== 用户信息 =====
    private Long userId;
    private String nickname;
    private String avatar;
    private String gender;
    private Integer age;
    private Boolean isOnline;
    private Boolean isVerified;

    // ===== 技能信息 =====
    private String skillType;
    private String skillTypeName;
    private String gameArea;
    private String rank;
    private Integer rankScore;
    private List<String> positions;

    // ===== 标签 =====
    private List<SkillTagVo> tags;

    // ===== 价格 =====
    private BigDecimal price;
    private String priceUnit;
    private String priceDisplay;

    // ===== 描述 =====
    private String description;

    // ===== 统计 =====
    private Integer orderCount;
    private BigDecimal rating;
    private Integer reviewCount;

    // ===== 距离 (由调用方计算或传入) =====
    private Integer distance;

    @Data
    public static class SkillTagVo implements Serializable {
        @Serial
        private static final long serialVersionUID = 1L;
        private String text;
        private String type;
        private String color;
    }
}
```

**文件**: `ruoyi-api/xypai-api-appuser/src/main/java/org/dromara/appuser/api/domain/vo/SkillServicePageResult.java`

```java
package org.dromara.appuser.api.domain.vo;

import lombok.Data;
import java.io.Serial;
import java.io.Serializable;
import java.util.List;

/**
 * 技能服务分页结果
 */
@Data
public class SkillServicePageResult implements Serializable {

    @Serial
    private static final long serialVersionUID = 1L;

    /** 技能类型信息 */
    private SkillTypeInfo skillType;

    /** Tab 列表 */
    private List<TabInfo> tabs;

    /** 筛选配置 */
    private FilterConfig filters;

    /** 数据列表 */
    private List<SkillServiceVo> list;

    /** 总数 */
    private Long total;

    /** 是否有更多 */
    private Boolean hasMore;

    @Data
    public static class SkillTypeInfo implements Serializable {
        @Serial
        private static final long serialVersionUID = 1L;
        private String type;
        private String label;
        private String icon;
    }

    @Data
    public static class TabInfo implements Serializable {
        @Serial
        private static final long serialVersionUID = 1L;
        private String value;
        private String label;
        private Integer count;
    }

    @Data
    public static class FilterConfig implements Serializable {
        @Serial
        private static final long serialVersionUID = 1L;
        private List<OptionInfo> sortOptions;
        private List<OptionInfo> genderOptions;
        private List<OptionInfo> statusOptions;
        private List<OptionInfo> gameAreas;
        private List<OptionInfo> ranks;
        private List<PriceRangeInfo> priceRanges;
        private List<OptionInfo> positions;
        private List<OptionInfo> tags;
    }

    @Data
    public static class OptionInfo implements Serializable {
        @Serial
        private static final long serialVersionUID = 1L;
        private String value;
        private String label;
    }

    @Data
    public static class PriceRangeInfo implements Serializable {
        @Serial
        private static final long serialVersionUID = 1L;
        private String value;
        private String label;
        private Integer min;
        private Integer max;
    }
}
```

### 1.3 扩展 RemoteAppUserService 接口

**文件**: `ruoyi-api/xypai-api-appuser/src/main/java/org/dromara/appuser/api/RemoteAppUserService.java`

```java
// 在现有接口中新增以下方法:

/**
 * 查询技能服务列表
 *
 * @param queryDto 查询条件
 * @return 分页结果
 */
SkillServicePageResult querySkillServiceList(SkillServiceQueryDto queryDto);

/**
 * 获取技能服务详情
 *
 * @param skillId 技能ID
 * @param userId 当前用户ID (可选)
 * @return 技能服务详情
 */
SkillServiceDetailVo getSkillServiceDetail(Long skillId, Long userId);

/**
 * 获取技能服务评价列表
 *
 * @param skillId 技能ID
 * @param pageNum 页码
 * @param pageSize 每页数量
 * @param filterBy 筛选类型 (all/excellent/positive/negative)
 * @return 评价分页结果
 */
SkillServiceReviewPageResult getSkillServiceReviews(Long skillId, Integer pageNum, Integer pageSize, String filterBy);
```

---

## 🔧 Step 2: 领域服务实现

### 2.1 数据库表说明

技能服务依赖 `xypai_user` 数据库的以下表：

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `skill` | 技能主表 | id, user_id, skill_type, price, description |
| `skill_image` | 技能图片 | skill_id, image_url |
| `skill_promise` | 技能承诺 | skill_id, promise_text |
| `skill_available_time` | 可用时间 | skill_id, time_range |
| `xy_user` | 用户信息 | id, nickname, avatar, gender |
| `user_stats` | 用户统计 | user_id, order_count, rating |

### 2.2 Mapper 新增查询方法

**文件**: `xypai-modules/xypai-user/src/main/java/org/dromara/user/mapper/SkillMapper.java`

```java
/**
 * 分页查询技能服务列表
 */
Page<SkillServiceVo> selectSkillServicePage(
    @Param("page") Page<?> page,
    @Param("query") SkillServiceQueryDto query
);

/**
 * 获取技能服务详情
 */
SkillServiceDetailVo selectSkillServiceDetail(@Param("skillId") Long skillId);

/**
 * 统计各 Tab 数量
 */
Map<String, Integer> countByTabs(@Param("skillType") String skillType);
```

### 2.3 Service 实现

**文件**: `xypai-modules/xypai-user/src/main/java/org/dromara/user/service/remote/RemoteAppUserServiceImpl.java`

```java
@Override
public SkillServicePageResult querySkillServiceList(SkillServiceQueryDto queryDto) {
    // 1. 查询技能列表
    Page<SkillServiceVo> page = skillMapper.selectSkillServicePage(
        new Page<>(queryDto.getPageNum(), queryDto.getPageSize()),
        queryDto
    );

    // 2. 构建筛选配置 (可缓存)
    FilterConfig filters = buildFilterConfig(queryDto.getSkillType());

    // 3. 统计各Tab数量
    Map<String, Integer> tabCounts = skillMapper.countByTabs(queryDto.getSkillType());

    // 4. 组装结果
    SkillServicePageResult result = new SkillServicePageResult();
    result.setList(page.getRecords());
    result.setTotal(page.getTotal());
    result.setHasMore(page.hasNext());
    result.setFilters(filters);
    result.setTabs(buildTabs(tabCounts));
    result.setSkillType(buildSkillTypeInfo(queryDto.getSkillType()));

    return result;
}
```

---

## 🔧 Step 3: BFF 层集成

### 3.1 修改 SkillServiceServiceImpl

**文件**: `xypai-aggregation/xypai-app-bff/src/main/java/org/dromara/appbff/service/impl/SkillServiceServiceImpl.java`

```java
package org.dromara.appbff.service.impl;

import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.apache.dubbo.config.annotation.DubboReference;
import org.dromara.appbff.domain.dto.ServiceListQueryDTO;
import org.dromara.appbff.domain.dto.ServiceReviewQueryDTO;
import org.dromara.appbff.domain.vo.*;
import org.dromara.appbff.service.SkillServiceService;
import org.dromara.appuser.api.RemoteAppUserService;
import org.dromara.appuser.api.domain.dto.SkillServiceQueryDto;
import org.dromara.appuser.api.domain.vo.*;
import org.springframework.stereotype.Service;

/**
 * 技能服务服务实现类（RPC 版本）
 */
@Slf4j
@Service
@RequiredArgsConstructor
public class SkillServiceServiceImpl implements SkillServiceService {

    @DubboReference
    private RemoteAppUserService remoteAppUserService;

    @Override
    public ServiceListResultVO queryServiceList(ServiceListQueryDTO queryDTO, Long userId) {
        log.info("查询服务列表: queryDTO={}, userId={}", queryDTO, userId);

        // 1. 转换请求 DTO
        SkillServiceQueryDto rpcQuery = convertToRpcQuery(queryDTO, userId);

        // 2. 调用 RPC 接口
        SkillServicePageResult rpcResult = remoteAppUserService.querySkillServiceList(rpcQuery);

        // 3. 转换响应 VO
        return convertToListResult(rpcResult);
    }

    @Override
    public ServiceDetailVO getServiceDetail(Long serviceId, Long userId) {
        log.info("获取服务详情: serviceId={}, userId={}", serviceId, userId);

        // 调用 RPC 接口
        SkillServiceDetailVo rpcResult = remoteAppUserService.getSkillServiceDetail(serviceId, userId);

        // 转换响应 VO
        return convertToDetailVO(rpcResult);
    }

    @Override
    public ServiceReviewListVO getServiceReviews(ServiceReviewQueryDTO queryDTO) {
        log.info("获取服务评价列表: queryDTO={}", queryDTO);

        // 调用 RPC 接口
        SkillServiceReviewPageResult rpcResult = remoteAppUserService.getSkillServiceReviews(
            queryDTO.getServiceId(),
            queryDTO.getPageNum(),
            queryDTO.getPageSize(),
            queryDTO.getFilterBy()
        );

        // 转换响应 VO
        return convertToReviewListVO(rpcResult, queryDTO.getServiceId());
    }

    // ========== 转换方法 ==========

    private SkillServiceQueryDto convertToRpcQuery(ServiceListQueryDTO dto, Long userId) {
        SkillServiceQueryDto query = new SkillServiceQueryDto();
        query.setSkillType(dto.getSkillType());
        query.setPageNum(dto.getPageNum());
        query.setPageSize(dto.getPageSize());
        query.setTabType(dto.getTabType());
        query.setSortBy(dto.getSortBy());
        query.setCurrentUserId(userId);

        if (dto.getFilters() != null) {
            ServiceListQueryDTO.ServiceFilterDTO f = dto.getFilters();
            query.setGender(f.getGender());
            query.setStatus(f.getStatus());
            query.setGameArea(f.getGameArea());
            query.setRanks(f.getRank());
            query.setPriceRanges(f.getPriceRange());
            query.setPositions(f.getPosition());
            query.setTags(f.getTags());
            query.setCityCode(f.getCityCode());
        }

        return query;
    }

    private ServiceListResultVO convertToListResult(SkillServicePageResult rpc) {
        ServiceListResultVO result = new ServiceListResultVO();
        // ... 字段映射逻辑
        return result;
    }

    private ServiceDetailVO convertToDetailVO(SkillServiceDetailVo rpc) {
        ServiceDetailVO result = new ServiceDetailVO();
        // ... 字段映射逻辑
        return result;
    }

    private ServiceReviewListVO convertToReviewListVO(SkillServiceReviewPageResult rpc, Long serviceId) {
        ServiceReviewListVO result = new ServiceReviewListVO();
        result.setServiceId(serviceId);
        // ... 字段映射逻辑
        return result;
    }
}
```

---

## 🧪 Step 4: 测试验证

### 4.1 单元测试

```java
@SpringBootTest
public class SkillServiceServiceImplTest {

    @Autowired
    private SkillServiceService skillServiceService;

    @Test
    void testQueryServiceList() {
        ServiceListQueryDTO query = new ServiceListQueryDTO();
        query.setSkillType("王者荣耀");
        query.setPageNum(1);
        query.setPageSize(10);

        ServiceListResultVO result = skillServiceService.queryServiceList(query, 1L);

        assertNotNull(result);
        assertNotNull(result.getList());
        assertTrue(result.getTotal() >= 0);
    }
}
```

### 4.2 集成测试

使用已有的测试文件:
- `Page11_ServiceListTest.java`
- `Page12_ServiceDetailTest.java`

---

## 📊 数据映射关系

### BFF VO → RPC VO 字段映射

| BFF 字段 | RPC 字段 | 说明 |
|----------|----------|------|
| `ServiceCardVO.serviceId` | `SkillServiceVo.skillId` | 服务ID |
| `ServiceCardVO.provider.userId` | `SkillServiceVo.userId` | 用户ID |
| `ServiceCardVO.provider.nickname` | `SkillServiceVo.nickname` | 昵称 |
| `ServiceCardVO.skillInfo.skillType` | `SkillServiceVo.skillType` | 技能类型 |
| `ServiceCardVO.price.amount` | `SkillServiceVo.price` | 价格 |
| `ServiceCardVO.stats.orders` | `SkillServiceVo.orderCount` | 订单数 |

---

## ⚠️ 注意事项

1. **分页参数**: pageNum 从 1 开始，与前端保持一致
2. **Null 安全**: RPC 返回值可能为 null，需要做判空处理
3. **距离计算**: 如需计算距离，需要调用 `RemoteLocationService.calculateBatchDistance()`
4. **缓存策略**: 筛选配置可缓存 5 分钟
5. **异常处理**: RPC 调用失败时返回空结果或降级处理

---

## 📅 预计工时

| 任务 | 工时 | 负责人 |
|------|------|--------|
| RPC 接口定义 (DTO/VO) | 0.5 天 | |
| 领域服务实现 | 1 天 | |
| BFF 层集成 | 0.5 天 | |
| 测试验证 | 0.5 天 | |
| **总计** | **2.5 天** | |

---

## 🔗 相关文档

- [ruoyi-api 快速理解](../../ruoyi-api/快速理解.md)
- [xypai-user 快速理解](../../xypai-modules/xypai-user/快速理解.md)
- [BFF 快速理解](../快速理解.md)
- [前端文档: 11-服务列表页面.md](前端文档链接)
- [前端文档: 12-服务详情页面.md](前端文档链接)

---

**文档版本**: v1.0.0
**最后更新**: 2025-11-26
