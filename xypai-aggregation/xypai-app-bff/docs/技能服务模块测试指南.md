# 技能服务模块测试指南

> 创建日期: 2025-11-26
> 模块: 技能服务 (Skill Service)
> 测试类型: 集成测试 + 单元测试

---

## 📋 目录

1. [当前测试状态](#当前测试状态)
2. [测试策略](#测试策略)
3. [快速测试 (现在就可以运行)](#快速测试)
4. [完整测试 (实现数据库后)](#完整测试)
5. [测试数据准备](#测试数据准备)
6. [常见问题](#常见问题)

---

## 当前测试状态

### ✅ 已完成
- RPC 接口定义完整
- BFF 层集成完成
- 错误处理和降级策略
- 现有测试文件可以运行

### ⏳ 待完成
- 数据库查询逻辑实现
- 测试数据准备
- 详细断言验证

---

## 测试策略

### 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **方案1: 使用现有测试** | • 无需修改<br>• 验证不破坏现有功能<br>• 测试降级策略 | • 只返回空数据<br>• 无法验证业务逻辑 | ⭐⭐⭐⭐⭐ (当前推荐) |
| **方案2: 添加测试数据** | • 完整业务验证<br>• 真实数据测试 | • 需要准备数据<br>• 需要实现查询 | ⭐⭐⭐⭐ (后续推荐) |
| **方案3: 单元测试** | • 隔离测试<br>• 快速反馈 | • 需要 Mock<br>• 覆盖不全面 | ⭐⭐⭐ (可选) |

---

## 快速测试

### 前置条件

**无需新建数据库!** 当前可以直接运行测试。

**需要的服务** (全部启动):
```bash
✓ Gateway (8080)
✓ xypai-auth (8200)
✓ xypai-app-bff (9400)
✓ xypai-user (9401)
✓ Nacos
✓ Redis
```

### 运行测试

#### 方式1: 命令行测试 (推荐)

```bash
# 切换到 BFF 目录
cd e:/Users/Administrator/Documents/GitHub/RuoYi-Cloud-Plus/xypai-aggregation/xypai-app-bff

# 测试服务列表接口
mvn test -Dtest=Page11_ServiceListTest

# 测试服务详情接口
mvn test -Dtest=Page12_ServiceDetailTest

# 一次运行两个测试
mvn test -Dtest=Page11_ServiceListTest,Page12_ServiceDetailTest
```

#### 方式2: 使用测试脚本

```bash
# Windows (Git Bash)
bash test-skill-service.sh

# Windows (PowerShell)
# (需要先创建 .ps1 版本)
```

#### 方式3: IDE 运行

在 IDEA 中:
1. 打开 `Page11_ServiceListTest.java`
2. 右键 → Run 'Page11_ServiceListTest'
3. 查看测试结果

### 预期结果

#### ✅ 测试通过 (当前阶段)

```
[INFO] Tests run: 12, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

**说明**:
- HTTP 200 响应
- 返回空数据列表 `{"list": [], "total": 0}`
- 降级策略生效

#### ❌ 测试失败

可能原因:
1. 服务未启动 → 检查 Nacos 注册
2. 端口冲突 → 检查 8080, 9400, 9401
3. 编译错误 → 运行 `mvn clean compile`

---

## 完整测试

### 阶段2: 实现数据库查询后

#### 1. 准备测试数据

创建测试数据文件: `test-data-skills.sql`

```sql
-- ========================================
-- 测试数据: 技能服务模块
-- ========================================

-- 清理旧数据 (可选)
-- DELETE FROM skills WHERE skill_id >= 2000 AND skill_id < 3000;
-- DELETE FROM xy_user WHERE user_id >= 1000 AND user_id < 2000;

-- ========== 1. 测试用户 ==========
INSERT INTO xy_user (user_id, mobile, country_code, nickname, avatar, gender, birthday, bio, city_code, district_code, created_at) VALUES
-- 用户1: 王者荣耀大神 (女, 在线)
(1001, '13900001001', '+86', '小糖糖', 'https://picsum.photos/100/100?random=1001', 'female', '2000-01-15', '王者荣耀荣耀王者，带飞小姐姐~', '110000', '110101', NOW()),

-- 用户2: 王者荣耀职业选手 (男, 在线)
(1002, '13900001002', '+86', '打野之王', 'https://picsum.photos/100/100?random=1002', 'male', '1998-05-20', '职业打野，100%胜率', '110000', '110105', NOW()),

-- 用户3: 台球教练 (男, 离线)
(1003, '13900001003', '+86', '台球大师', 'https://picsum.photos/100/100?random=1003', 'male', '1990-08-10', '专业台球教练，10年经验', '110000', '110108', NOW()),

-- 用户4: 王者荣耀中路 (女, 在线)
(1004, '13900001004', '+86', '中路女神', 'https://picsum.photos/100/100?random=1004', 'female', '2001-03-25', '法师专精，带妹上分', '110000', '110102', NOW());

-- ========== 2. 用户统计 ==========
INSERT INTO user_stats (user_id, fans_count, likes_count, skill_count, order_count) VALUES
(1001, 500, 1200, 2, 120),
(1002, 800, 2000, 1, 200),
(1003, 300, 600, 1, 80),
(1004, 650, 1500, 1, 150);

-- ========== 3. 技能服务 ==========
INSERT INTO skills (skill_id, user_id, skill_name, skill_type, cover_image, description, price, price_unit, is_online, rating, review_count, order_count, game_name, game_rank, service_hours, created_at) VALUES
-- 用户1的技能 (王者荣耀 - 荣耀王者)
(2001, 1001, '王者荣耀陪玩', 'online', 'https://picsum.photos/400/200?random=2001',
 '荣耀王者段位，擅长打野和辅助，声音甜美，技术过硬，带你轻松上分！',
 15.00, '局', 1, 4.8, 50, 120, '王者荣耀', '荣耀王者', 5.0, NOW()),

(2002, 1001, '王者荣耀代练', 'online', 'https://picsum.photos/400/200?random=2002',
 '接代练单，保证胜率，价格实惠',
 30.00, '局', 1, 4.9, 30, 80, '王者荣耀', '荣耀王者', 3.0, NOW()),

-- 用户2的技能 (王者荣耀 - 最强王者)
(2003, 1002, '王者荣耀职业陪练', 'online', 'https://picsum.photos/400/200?random=2003',
 '职业选手，专精打野位置，教你意识和操作',
 50.00, '小时', 1, 5.0, 80, 200, '王者荣耀', '最强王者', 2.0, NOW()),

-- 用户3的技能 (台球 - 线下)
(2004, 1003, '台球一对一教学', 'offline', 'https://picsum.photos/400/200?random=2004',
 '10年台球教学经验，一对一指导，快速提升球技',
 100.00, '小时', 1, 4.9, 30, 80, NULL, NULL, 2.0, NOW()),

-- 用户4的技能 (王者荣耀 - 星耀)
(2005, 1004, '王者荣耀中路带飞', 'online', 'https://picsum.photos/400/200?random=2005',
 '中路法师专精，擅长妲己、嫦娥、不知火舞',
 12.00, '局', 1, 4.7, 60, 150, '王者荣耀', '星耀', 4.0, NOW());

-- ========== 4. 技能图片 ==========
INSERT INTO skill_images (skill_id, image_url, display_order) VALUES
(2001, 'https://picsum.photos/400/300?random=20011', 1),
(2001, 'https://picsum.photos/400/300?random=20012', 2),
(2003, 'https://picsum.photos/400/300?random=20031', 1);

-- ========== 5. 技能承诺 ==========
INSERT INTO skill_promises (skill_id, promise_text, display_order) VALUES
(2001, '技术保证', 1),
(2001, '服务态度好', 2),
(2001, '准时守信', 3),
(2003, '职业教学', 1),
(2003, '效果显著', 2);

-- ========== 6. 评价数据 (可选 - 如果有 reviews 表) ==========
-- INSERT INTO reviews (review_id, skill_id, user_id, rating, content, created_at) VALUES
-- (3001, 2001, 9001, 5, '小姐姐声音超甜，技术也很好！', NOW()),
-- (3002, 2001, 9002, 5, '带飞了，下次还找你！', NOW() - INTERVAL 1 DAY);

-- ========== 完成 ==========
SELECT '✅ 测试数据插入完成!' AS status;
SELECT COUNT(*) AS user_count FROM xy_user WHERE user_id BETWEEN 1000 AND 2000;
SELECT COUNT(*) AS skill_count FROM skills WHERE skill_id BETWEEN 2000 AND 3000;
```

#### 2. 导入测试数据

```bash
# 方式1: MySQL 命令行
mysql -u root -p xypai_user < test-data-skills.sql

# 方式2: Navicat/DBeaver
# 直接粘贴 SQL 执行

# 方式3: Spring Boot Test
# 使用 @Sql 注解自动导入
```

#### 3. 实现数据库查询

在 `RemoteAppUserServiceImpl.java` 中实现 TODO 方法:

```java
@Override
public SkillServicePageResult querySkillServiceList(SkillServiceQueryDto queryDto) {
    // 1. 查询技能列表
    Page<Skill> page = new Page<>(queryDto.getPageNum(), queryDto.getPageSize());
    LambdaQueryWrapper<Skill> wrapper = new LambdaQueryWrapper<>();

    // 应用筛选条件
    if (queryDto.getGender() != null) {
        // JOIN users 表筛选性别
    }
    // ... 更多筛选逻辑

    Page<Skill> result = skillMapper.selectPage(page, wrapper);

    // 2. 转换为 VO
    List<SkillServiceVo> vos = result.getRecords().stream()
        .map(this::convertToSkillServiceVo)
        .collect(Collectors.toList());

    // 3. 构建返回结果
    return SkillServicePageResult.builder()
        .list(vos)
        .total(result.getTotal())
        .hasMore(result.hasNext())
        .build();
}
```

#### 4. 重新运行测试

```bash
mvn test -Dtest=Page11_ServiceListTest

# 预期: 返回真实数据
# - list 不为空
# - total > 0
# - 数据字段完整
```

#### 5. 验证数据正确性

测试中添加详细断言:

```java
@Test
void testGetServiceList_HonorOfKings() {
    ResponseEntity<Map> response = restTemplate.exchange(url, ...);

    Map data = (Map) response.getBody().get("data");
    List list = (List) data.get("list");

    // 验证数据不为空
    assertTrue(list.size() > 0, "应该返回至少1条数据");

    // 验证第一条数据结构
    Map firstItem = (Map) list.get(0);
    assertNotNull(firstItem.get("serviceId"));
    assertNotNull(firstItem.get("provider"));
    assertNotNull(firstItem.get("price"));
    assertNotNull(firstItem.get("stats"));

    // 验证筛选生效
    Map provider = (Map) firstItem.get("provider");
    assertEquals("female", provider.get("gender"), "性别筛选应生效");
}
```

---

## 测试数据准备

### 方式1: 手动插入 SQL ✅ (推荐)

**优点**:
- 完全控制数据
- 可重复使用
- 适合 CI/CD

**步骤**:
1. 使用上面的 `test-data-skills.sql`
2. 导入到 `xypai_user` 数据库
3. 运行测试

### 方式2: 使用 Faker 库生成

**优点**:
- 自动生成大量数据
- 数据多样化

**缺点**:
- 需要额外依赖
- 数据不可控

```xml
<!-- 添加到 pom.xml -->
<dependency>
    <groupId>com.github.javafaker</groupId>
    <artifactId>javafaker</artifactId>
    <version>1.0.2</version>
    <scope>test</scope>
</dependency>
```

### 方式3: Spring Boot @Sql 注解

```java
@SpringBootTest
@Sql(scripts = "/test-data-skills.sql", executionPhase = Sql.ExecutionPhase.BEFORE_TEST_METHOD)
@Sql(scripts = "/cleanup-test-data.sql", executionPhase = Sql.ExecutionPhase.AFTER_TEST_METHOD)
public class SkillServiceIntegrationTest {
    // 测试代码
}
```

---

## 常见问题

### Q1: 测试失败 - Connection refused

**原因**: 服务未启动

**解决**:
```bash
# 检查服务状态
jps -l | grep xypai

# 启动服务
cd ruoyi-gateway && mvn spring-boot:run
cd xypai-auth && mvn spring-boot:run
cd xypai-app-bff && mvn spring-boot:run
cd xypai-user && mvn spring-boot:run
```

### Q2: 测试通过但返回空数据

**原因**: 数据库查询逻辑未实现 (预期行为)

**解决**: 这是正常的! 降级策略生效，等待实现数据库查询后会返回真实数据。

### Q3: 需要新建数据库吗?

**回答**: **不需要!**

当前阶段:
- 使用现有 `xypai_user` 数据库
- 测试会通过 (返回空数据)

后续阶段:
- 在 `xypai_user` 中添加测试数据
- 无需新建数据库

### Q4: 如何清理测试数据?

```sql
-- 清理测试数据
DELETE FROM skills WHERE skill_id BETWEEN 2000 AND 3000;
DELETE FROM skill_images WHERE skill_id BETWEEN 2000 AND 3000;
DELETE FROM skill_promises WHERE skill_id BETWEEN 2000 AND 3000;
DELETE FROM user_stats WHERE user_id BETWEEN 1000 AND 2000;
DELETE FROM xy_user WHERE user_id BETWEEN 1000 AND 2000;
```

### Q5: 如何调试 RPC 调用?

**方式1: 查看日志**
```bash
tail -f logs/xypai-user/info.log | grep SkillService
tail -f logs/xypai-app-bff/info.log | grep SkillService
```

**方式2: 使用 Dubbo Admin**
- 访问 http://localhost:38080 (如果安装了)
- 查看服务调用情况

**方式3: IDE 断点调试**
- 在 `SkillServiceServiceImpl.java` 的 RPC 调用处打断点
- 在 `RemoteAppUserServiceImpl.java` 的方法入口打断点

---

## 总结

### 当前可以做什么 ✅

1. **运行现有测试** - 验证 RPC 集成不破坏现有功能
2. **验证降级策略** - 确认错误处理正常工作
3. **检查编译通过** - 确保代码无语法错误

```bash
mvn clean test -Dtest=Page11_ServiceListTest,Page12_ServiceDetailTest
```

### 后续需要做什么 🔜

1. **实现数据库查询** - 完成 3 个 TODO 方法
2. **准备测试数据** - 导入 test-data-skills.sql
3. **验证业务逻辑** - 重新运行测试，确认返回真实数据

### 测试清单

- [ ] 运行 Page11_ServiceListTest (当前阶段)
- [ ] 运行 Page12_ServiceDetailTest (当前阶段)
- [ ] 准备测试数据 SQL (实现数据库后)
- [ ] 实现 querySkillServiceList() (实现数据库后)
- [ ] 实现 getSkillServiceDetail() (实现数据库后)
- [ ] 实现 getSkillServiceReviews() (实现数据库后)
- [ ] 重新运行所有测试 (实现数据库后)
- [ ] 添加边界测试 (可选)
- [ ] 性能测试 (可选)

---

**文档版本**: v1.0.0
**最后更新**: 2025-11-26
**创建人**: Claude Code
