# XyPai-App-BFF App 端业务编排服务快速理解

## ⚠️ 重要变更 (2025-11-25)

### 包结构已统一
**问题**: 之前存在包结构不一致导致 `NoClassDefFoundError`
- ❌ 旧包: `org.dromara.aggregation.*`
- ✅ 新包: `org.dromara.appbff.*` (已统一)

**已修复**:
- ✅ 主启动类: `org.dromara.appbff.XyPaiAppBffApplication`
- ✅ 所有 Controller/Service/DTO/VO 在 `org.dromara.appbff` 包下
- ✅ 所有测试文件已迁移到 `org.dromara.appbff` 和 `org.dromara.appbff.pages` 包下
- ✅ pom.xml 已配置 `<mainClass>org.dromara.appbff.XyPaiAppBffApplication</mainClass>`

**影响**: 如果之前构建失败或测试失败，现在应该正常了

---

## 📌 核心定位
向娱拍 App 端业务编排服务（Backend For Frontend），基于 Spring Cloud 微服务架构。

**本模块职责**：为 App 客户端提供聚合接口，不直接连接数据库，通过 Dubbo RPC 调用多个领域服务（user、content、order 等），实现业务编排、数据聚合、推荐算法。

## 🎯 主要功能
- **首页 Feed 流**：用户推荐（线上/线下）+ 距离计算 + 技能匹配
- **发现页聚合**：内容流 + 用户信息 + 互动数据一次返回
- **用户主页聚合**：用户资料 + 动态列表 + 技能 + 粉丝数统计
- **订单详情聚合**：订单 + 用户信息 + 支付信息完整返回
- **推荐算法**：基于距离、技能、活跃度的用户匹配引擎
- **组局中心**：活动列表、活动详情、发布组局、报名管理（✅ Mock已实现）
- **技能服务**：服务列表、服务详情、评价列表（✅ Mock已实现）

## 🏗️ 技术栈
```
Spring Boot 3.x + Dubbo RPC + Nacos
Redis (推荐结果缓存 + 用户画像缓存)
```

## 📁 核心目录结构
```
xypai-app-bff/
└── src/main/java/org/dromara/appbff/    # ✅ 统一包名
    ├── XyPaiAppBffApplication.java      # ✅ Spring Boot 启动类
    ├── controller/
    │   ├── HomeFeedController.java      # ✅ 首页 Feed 流（Mock）
    │   ├── HomeFilterController.java    # ✅ 首页筛选（Mock）
    │   ├── HomeLimitedTimeController.java # ✅ 限时专享（RPC真实数据）
    │   ├── HomeSearchController.java    # ✅ 搜索初始化（Mock）
    │   ├── SearchResultController.java  # ✅ 搜索结果（Mock）
    │   ├── ActivityController.java      # ✅ 组局中心（Mock）
    │   └── SkillServiceController.java # ✅ 技能服务（Mock）
    ├── service/
    │   ├── HomeFilterService.java       # 筛选服务接口
    │   ├── HomeLimitedTimeService.java  # 限时专享服务接口
    │   ├── HomeSearchService.java       # 搜索服务接口
    │   ├── ActivityService.java         # ✅ 组局活动服务接口
    │   ├── SkillServiceService.java     # ✅ 技能服务接口
    │   └── impl/
    │       ├── HomeFilterServiceImpl.java
    │       ├── HomeLimitedTimeServiceImpl.java  # ✅ 真实RPC调用
    │       ├── HomeSearchServiceImpl.java
    │       ├── HomeSearchResultServiceImpl.java
    │       ├── ActivityServiceImpl.java # ✅ 组局活动服务（Mock）
    │       └── SkillServiceServiceImpl.java # ✅ 技能服务（Mock）
    └── domain/
        ├── dto/
        │   ├── HomeFeedQueryDTO.java
        │   ├── FilterApplyDTO.java      # 筛选请求DTO（含内部类FilterCriteria）
        │   ├── LimitedTimeQueryDTO.java
        │   ├── SearchQueryDTO.java
        │   ├── SearchHistoryDeleteDTO.java
        │   ├── ActivityListQueryDTO.java    # ✅ 活动列表查询
        │   ├── ActivityPublishDTO.java      # ✅ 发布组局请求
        │   ├── ActivityRegisterDTO.java     # ✅ 活动报名请求
        │   ├── ActivityRegisterCancelDTO.java # ✅ 取消报名请求
        │   ├── ActivityPayDTO.java          # ✅ 活动付款请求
        │   ├── ServiceListQueryDTO.java     # ✅ 服务列表查询
        │   └── ServiceReviewQueryDTO.java   # ✅ 服务评价查询
        └── vo/
            ├── UserCardVO.java
            ├── FilterConfigVO.java
            ├── FilterResultVO.java
            ├── LimitedTimeResultVO.java
            ├── LimitedTimeUserVO.java
            ├── SearchInitVO.java
            ├── SearchResultVO.java
            ├── ActivityCardVO.java          # ✅ 活动卡片（列表用）
            ├── ActivityListResultVO.java    # ✅ 活动列表结果
            ├── ActivityDetailVO.java        # ✅ 活动详情
            ├── ActivityPublishConfigVO.java # ✅ 发布配置
            ├── ActivityPublishResultVO.java # ✅ 发布结果
            ├── ActivityRegisterResultVO.java # ✅ 报名结果
            ├── ActivityPayResultVO.java     # ✅ 支付结果
            ├── ServiceCardVO.java           # ✅ 服务卡片（列表用）
            ├── ServiceListResultVO.java     # ✅ 服务列表结果
            ├── ServiceDetailVO.java         # ✅ 服务详情
            └── ServiceReviewListVO.java     # ✅ 服务评价列表

└── src/test/java/org/dromara/appbff/    # ✅ 测试文件已迁移
    ├── AppHomeFeedTest.java
    └── pages/
        ├── Page02_FilterTest.java       # ✅ 筛选页面集成测试
        ├── Page02_PublishFeedTest.java
        ├── Page03_FeedDetailTest.java   # ✅ 动态详情集成测试
        ├── Page05_LimitedTimeTest.java  # ✅ 限时专享集成测试
        ├── Page06_SearchTest.java       # ✅ 搜索页面集成测试
        ├── Page07_SearchResultsTest.java # ✅ 搜索结果集成测试
        ├── Page08_ActivityListTest.java  # ✅ 组局列表集成测试
        ├── Page09_ActivityDetailTest.java # ✅ 组局详情集成测试
        ├── Page10_PublishActivityTest.java # ✅ 发布组局集成测试
        ├── Page11_ServiceListTest.java   # ✅ 服务列表集成测试
        └── Page12_ServiceDetailTest.java # ✅ 服务详情集成测试
```

## 🔑 核心接口

### 首页相关
| 接口 | 说明 | 聚合数据来源 | 状态 |
|------|------|-------------|------|
| `GET /api/home/feed` | 首页用户推荐流 | user(用户列表) + content(动态数) | ✅ Mock |
| `GET /api/home/filter/config` | 获取筛选配置 | - (Mock数据) | ✅ Mock |
| `POST /api/home/filter/apply` | 应用筛选条件 | - (Mock数据) | ✅ Mock |
| `GET /api/home/limited-time/list` | **限时专享列表** | xypai-user (users+skills+stats) | ✅ **RPC真实数据** |
| `GET /api/home/search/init` | 搜索页初始化 | - (Mock数据) | ✅ Mock |
| `GET /api/home/search/suggest` | 搜索联想 | - (Mock数据) | ✅ Mock |
| `GET /api/v1/search/all` | 综合搜索 | - (Mock数据) | ✅ Mock |
| `GET /api/v1/search/user` | 用户搜索 | - (Mock数据) | ✅ Mock |

### 组局中心 (✅ 新增)
| 接口 | 说明 | 聚合数据来源 | 状态 |
|------|------|-------------|------|
| `GET /api/activity/list` | 活动列表（支持筛选排序） | - (Mock数据) | ✅ Mock |
| `GET /api/activity/detail/{id}` | 活动详情 | - (Mock数据) | ✅ Mock |
| `POST /api/activity/register` | 报名参加活动 | - (Mock数据) | ✅ Mock |
| `POST /api/activity/register/cancel` | 取消报名 | - (Mock数据) | ✅ Mock |
| `GET /api/activity/publish/config` | 发布配置（类型/费用/规则） | - (Mock数据) | ✅ Mock |
| `POST /api/activity/publish` | 发布组局活动 | - (Mock数据) | ✅ Mock |
| `POST /api/activity/pay` | 支付活动费用 | - (Mock数据) | ✅ Mock |
| `POST /api/activity/approve` | 审核报名申请（发起人） | - (Mock数据) | ✅ Mock |
| `POST /api/activity/cancel/{id}` | 取消活动（发起人） | - (Mock数据) | ✅ Mock |

### 技能服务 (✅ 新增)
| 接口 | 说明 | 聚合数据来源 | 状态 |
|------|------|-------------|------|
| `GET /api/service/list` | 服务列表（支持Tab/排序/筛选） | xypai-user (skill) | ✅ Mock |
| `GET /api/service/detail` | 服务详情（含评价摘要） | xypai-user (skill+user) | ✅ Mock |
| `GET /api/service/reviews` | 服务评价列表（分页） | xypai-user (review) | ✅ Mock |

### 待实现
| 接口 | 说明 | 聚合数据来源 | 状态 |
|------|------|-------------|------|
| `GET /api/discovery` | 发现页内容流 | content(动态) + user(作者) + interaction(互动) | ⏳ 待实现 |
| `GET /api/profile/{userId}/full` | 用户主页完整信息 | user(资料+技能+统计) + content(动态) | ⏳ 待实现 |
| `GET /api/order/detail/{orderId}` | 订单详情 | order(订单) + user(用户) + payment(支付) | ⏳ 待实现 |

## 🏛️ 微服务架构
```
┌─────────────────┐
│   App 客户端     │
└────────┬────────┘
         │ REST API
         ↓
┌─────────────────┐
│  Gateway (9300) │  ← 统一网关（认证、路由）
└────────┬────────┘
         │
    ┌────┴────────────┬────────────┐
    ↓                 ↓             ↓
┌──────────┐  ┌──────────┐  ┌──────────┐
│ xypai-   │  │ xypai-   │  │ xypai-   │
│ app-bff  │  │ auth     │  │ content  │
│ (9400)   │  │ (8200)   │  │ (9403)   │
└────┬─────┘  └──────────┘  └──────────┘
     │ Dubbo RPC
     ↓
┌──────────┐
│ xypai-   │  领域服务层
│ user     │  (数据库访问)
│ (9401)   │
└────┬─────┘
     │
   ┌─▼───┐
   │MySQL│  基础设施层
   │user │
   └─────┘
```

## 🔥 技术亮点
1. **BFF 模式**：前端一次请求获取完整数据，避免多次 HTTP 调用（从 5 次 → 1 次）
2. **微服务解耦**：聚合层不直接访问数据库，所有数据通过 Dubbo RPC 获取
3. **批量优化**：一次请求聚合多服务数据，减少前端 HTTP 调用次数
4. **数据裁剪**：根据场景返回必要字段，首页用户卡片仅返回 8 个字段（完整用户有 33 个字段）
5. **缓存优化**：热门推荐结果缓存 5 分钟，用户画像缓存 30 分钟
6. **包结构统一**：单一包结构 `org.dromara.appbff`，清晰明了

## 📦 RPC 依赖关系

### 已使用的 RPC 接口

| 接口模块 | 接口方法 | 用途 | 调用位置 | 状态 |
|----------|----------|------|----------|------|
| **RemoteAppUserService** | `queryLimitedTimeUsers()` | 限时专享用户列表 | `HomeLimitedTimeServiceImpl` | ✅ 已集成 |
| **RemoteAppUserService** | `getFilterConfig()` | 筛选配置选项 | `HomeFilterServiceImpl` | ✅ 已集成 |
| **RemoteAppUserService** | `queryFilteredUsers()` | 筛选用户列表 | `HomeFilterServiceImpl` | ✅ 已集成 |

### 待集成的 RPC 接口

```
xypai-app-bff (本模块)
  ↓ Dubbo RPC 调用
├─ xypai-user (RemoteAppUserService) ─────────────────────────────────
│   ├─ queryLimitedTimeUsers(...)             # ✅ 已集成 - 限时专享
│   ├─ getFilterConfig(type)                  # ✅ 已集成 - 筛选配置
│   ├─ queryFilteredUsers(FilterQueryDto)     # ✅ 已集成 - 筛选用户
│   ├─ getUserById(userId)                    # ⏳ 待集成 - 首页Feed
│   └─ registerOrGetByMobile(...)             # 由 xypai-auth 调用
│
├─ xypai-chat (RemoteChatService) ────────────────────────────────────
│   ├─ getUserUnreadCount(userId)             # ⏳ 待集成 - 消息未读数
│   ├─ getUserConversations(userId, limit)    # ⏳ 待集成 - 会话列表
│   └─ batchCheckOnlineStatus(userIds)        # ⏳ 待集成 - 在线状态
│
├─ xypai-common (4个服务) ────────────────────────────────────────────
│   ├─ RemoteLocationService
│   │   └─ calculateBatchDistance(...)        # ⏳ 待集成 - 距离计算
│   ├─ RemoteMediaService
│   │   └─ getFileUrl(fileId)                 # ⏳ 待集成 - 头像URL
│   ├─ RemoteNotificationService
│   │   └─ getUnreadCount(userId)             # ⏳ 待集成 - 通知未读
│   └─ RemoteReportService
│       └─ canUserPost(userId)                # ⏳ 待集成 - 发布权限
│
├─ xypai-order (RemoteOrderService) ──────────────────────────────────
│   └─ getOrderCount(request)                 # ⏳ 待集成 - 订单统计
│
└─ xypai-payment (RemotePaymentService) ──────────────────────────────
    └─ getBalance(userId)                     # ⏳ 待集成 - 余额查询
```

### RPC 接口定义位置
详见: `ruoyi-api/快速理解.md`

## 🚀 快速启动

### 前置条件
```bash
# 1. 确保依赖服务已启动
#    - Nacos（服务注册中心）
#    - Redis（缓存）
#    - MySQL（xypai_user 数据库）
#    - xypai-user (9401) - 用户服务
#    - xypai-content (9403) - 内容服务（可选）
#    - Gateway (9300) - 统一网关
```

### 启动服务
```bash
# 方式1: IDE 启动 (推荐)
# 主类: org.dromara.appbff.XyPaiAppBffApplication

# 方式2: 命令行启动
cd xypai-aggregation/xypai-app-bff
mvn clean package -DskipTests
java -jar target/xypai-app-bff.jar

# 默认端口：9400
# Swagger UI: http://localhost:9400/doc.html
```

### 验证服务
```bash
# 检查 Nacos 注册
# http://localhost:8848/nacos (nacos/nacos)

# 测试接口（需先登录获取 token）
GET http://localhost:9400/api/home/limited-time/list
```

## 📌 注意事项

### 关键要点
- ⚠️ **包结构统一**: 使用 `org.dromara.appbff.XyPaiAppBffApplication` 启动类
- ⚠️ **重新构建**: 包结构变更后必须执行 `mvn clean compile`
- **无数据库连接**：本模块是纯业务编排层，所有数据通过 Dubbo RPC 调用领域服务获取
- **依赖服务启动顺序**：必须先启动领域服务（user、content、order、payment），否则 RPC 调用失败
- **统一返回格式**：所有接口返回 `R<T>` (code, message, data)

### 性能优化建议
- 聚合接口应缓存高频数据（如首页推荐列表）
- 推荐算法可使用 Redis 缓存用户画像
- 批量 RPC 调用时注意超时控制（默认 3 秒）

### 开发规范
- BFF 层专注业务逻辑，领域服务专注数据 CRUD
- 职责清晰分离，避免代码耦合
- 使用 Mock 数据时注明 TODO，便于后续替换

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| `ruoyi-api/快速理解.md` | RPC 接口定义层文档（所有 Remote*Service 接口详解） |
| `xypai-aggregation/快速理解.md` | 聚合服务层总体文档 |
| `xypai-modules/xypai-user/快速理解.md` | 用户服务实现文档 |
| `向娱拍平台架构总览.md` | 项目整体架构文档 |

---

**最后更新**: 2025-11-26
**状态**: ✅ 包结构统一 + 筛选/限时专享 RPC 集成完成 + 组局中心 Mock 实现 + 技能服务 Mock 实现
**下一步**:
1. 技能服务 RPC 集成（接入 xypai-user 的 skill 相关接口）
2. 组局中心 RPC 集成（接入 activity 服务）
3. 首页 Feed 流 RPC 集成
4. 完善集成测试
