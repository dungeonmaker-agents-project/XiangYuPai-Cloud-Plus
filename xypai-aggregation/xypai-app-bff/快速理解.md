# XyPai-App-BFF App 端业务编排服务快速理解

## 📊 当前进度总览 (2025-12-04 更新)

### 测试文件数据来源状态

| 测试文件 | 页面功能 | 数据来源 | 状态 | 说明 |
|---------|---------|---------|------|------|
| `Page02_FilterTest` | 首页筛选 | **RPC 真实数据** | ✅ 已完成 | 调用 xypai-user 服务 |
| `Page02_PublishFeedTest` | 发布动态 | **HTTP 直连** | ✅ 已完成 | 直接调用 xypai-content |
| `Page03_FeedDetailTest` | 动态详情 | **HTTP 直连** | ✅ 已完成 | 直接调用 xypai-content |
| `Page05_LimitedTimeTest` | 限时专享 | **RPC 真实数据** | ✅ 已完成 | 调用 xypai-user 服务 |
| `Page06_SearchTest` | 搜索初始化 | Mock 数据 | ⏳ 待改造 | 热搜/历史/联想 |
| `Page07_SearchResultsTest` | 搜索结果 | Mock 数据 | ⏳ 待改造 | 综合/用户/话题搜索 |
| `Page08_ActivityListTest` | 组局列表 | **RPC 真实数据** | ✅ 已完成 | 调用 xypai-content RemoteActivityService |
| `Page09_ActivityDetailTest` | 组局详情 | **RPC 真实数据** | ✅ 已完成 | 调用 xypai-content RemoteActivityService |
| `Page10_PublishActivityTest` | 发布组局 | **RPC 真实数据** | ✅ 已完成 | 调用 xypai-content RemoteActivityService |
| `Page11_ServiceListTest` | 服务列表 | **RPC 真实数据** | ✅ 已完成 | 调用 xypai-user RemoteAppUserService |
| `Page12_ServiceDetailTest` | 服务详情 | **RPC 真实数据** | ✅ 已完成 | 调用 xypai-user RemoteAppUserService |
| `Page_OtherUserProfileTest` | 对方主页 | **RPC 真实数据** | ✅ 已完成 | 调用 xypai-user + xypai-content |

### 进度统计

```
总计测试文件: 12 个
├── ✅ RPC 真实数据: 8 个 (67%)  - Page02_Filter, Page05_LimitedTime, Page08-12, OtherUserProfile
├── ✅ HTTP 直连:    2 个 (17%)  - Page02_PublishFeed, Page03_FeedDetail
└── ⏳ Mock 数据:    2 个 (16%)  - Page06_Search, Page07_SearchResults
```

---

## ⚠️ 重要说明

### 什么是"真实数据"?

**真实数据** = 通过 RPC/HTTP 调用领域服务，从 MySQL 数据库查询返回

**Mock 数据** = BFF 层硬编码生成的假数据，与数据库无关

### 已完成 RPC 集成的接口

| 接口路径 | 服务 | RPC 方法 | 数据库表 |
|---------|------|---------|---------|
| `GET /api/home/limited-time/list` | xypai-user | `queryLimitedTimeUsers()` | users + skills + user_stats |
| `GET /api/home/filter/config` | xypai-user | `getFilterConfig()` | skills (聚合统计) |
| `POST /api/home/filter/apply` | xypai-user | `queryFilteredUsers()` | users + skills + user_stats |
| `GET /api/service/list` | xypai-user | `querySkillServiceList()` | users + skills (含 peak_score) |
| `GET /api/service/detail` | xypai-user | `getSkillServiceDetail()` | skills + users |
| `GET /api/service/reviews` | xypai-user | `getSkillServiceReviews()` | reviews + users |

---

## 📌 核心定位

向娱拍 App 端业务编排服务（Backend For Frontend），基于 Spring Cloud 微服务架构。

**本模块职责**：为 App 客户端提供聚合接口，不直接连接数据库，通过 Dubbo RPC 调用多个领域服务（user、content、order 等），实现业务编排、数据聚合、推荐算法。

---

## 🗓️ RPC 改造计划

### ✅ Phase 1: 技能服务 RPC 集成 (已完成)

**目标**: Page11-12 从 Mock 切换到真实数据

**已完成工作**:
1. ✅ 扩展 `RemoteAppUserService` 接口:
   - `querySkillServiceList()` - 技能服务列表
   - `getSkillServiceDetail()` - 技能服务详情
   - `getSkillServiceReviews()` - 评价列表

2. ✅ 在 xypai-user 服务中实现上述接口

3. ✅ 修改 BFF 层 `SkillServiceServiceImpl`:
   - 注入 `@DubboReference RemoteAppUserService`
   - 替换 Mock 数据为 RPC 调用

**涉及的数据库表**:
- `skills` - 技能主表
- `skill_image` - 技能图片
- `users` - 用户信息
- `user_stats` - 用户统计

### ⏳ Phase 2: 搜索功能 RPC 集成 (待开始)

**目标**: Page06-07 从 Mock 切换到真实数据

**需要的工作**:
1. 新建搜索相关数据库表:
   - `search_history` - 搜索历史
   - `hot_search_keywords` - 热门搜索

2. 扩展 RPC 接口:
   - `getHotSearchKeywords()` - 热门搜索词
   - `getUserSearchHistory()` - 用户搜索历史
   - `searchUsers()` - 搜索用户
   - `searchSkills()` - 搜索技能

3. 修改 BFF 层搜索服务实现

### ✅ Phase 3: 组局中心 RPC 集成 (已完成)

**目标**: Page08-10 从 Mock 切换到真实数据

**已完成工作**:
1. ✅ **架构决策**: 扩展 xypai-content 服务
2. ✅ 新建数据库表:
   - `activity` - 活动主表
   - `activity_participant` - 参与者表
   - `activity_type` - 活动类型

3. ✅ 新建 `RemoteActivityService` RPC 接口
4. ✅ 修改 BFF 层 `ActivityServiceImpl`

### ⏳ Phase 4: 首页 Feed 流 RPC 集成

**目标**: 实现真实的用户推荐算法

**需要的工作**:
1. 实现推荐算法 (距离、在线状态、活跃度)
2. 批量 RPC 调用优化
3. Redis 缓存层

---

## 📁 核心目录结构

```
xypai-app-bff/
└── src/main/java/org/dromara/appbff/
    ├── XyPaiAppBffApplication.java      # Spring Boot 启动类
    ├── controller/
    │   ├── HomeFeedController.java      # 首页 Feed 流 (Mock)
    │   ├── HomeFilterController.java    # 首页筛选 (✅ RPC)
    │   ├── HomeLimitedTimeController.java # 限时专享 (✅ RPC)
    │   ├── HomeSearchController.java    # 搜索初始化 (⏳ Mock)
    │   ├── SearchResultController.java  # 搜索结果 (⏳ Mock)
    │   ├── ActivityController.java      # 组局中心 (✅ RPC)
    │   ├── SkillServiceController.java  # 技能服务 (✅ RPC)
    │   └── OrderConfirmController.java   # 订单确认支付 (✅ RPC)
    ├── service/
    │   └── impl/
    │       ├── HomeFilterServiceImpl.java       # ✅ RPC 调用
    │       ├── HomeLimitedTimeServiceImpl.java  # ✅ RPC 调用
    │       ├── HomeSearchServiceImpl.java       # ⏳ Mock 数据
    │       ├── HomeSearchResultServiceImpl.java # ⏳ Mock 数据
    │       ├── ActivityServiceImpl.java         # ✅ RPC 调用 (RemoteActivityService)
    │       ├── SkillServiceServiceImpl.java     # ✅ RPC 调用 (RemoteAppUserService)
    │       └── OrderConfirmServiceImpl.java    # ✅ RPC 调用 (Payment+Order+User)
    └── domain/
        ├── dto/   # 请求 DTO
        └── vo/    # 响应 VO

└── src/test/java/org/dromara/appbff/pages/  # 集成测试
    ├── Page02_FilterTest.java       # ✅ 真实数据 (RPC)
    ├── Page02_PublishFeedTest.java  # ✅ 直连 content
    ├── Page03_FeedDetailTest.java   # ✅ 直连 content
    ├── Page05_LimitedTimeTest.java  # ✅ 真实数据 (RPC)
    ├── Page06_SearchTest.java       # ⏳ Mock
    ├── Page07_SearchResultsTest.java # ⏳ Mock
    ├── Page08_ActivityListTest.java  # ✅ 真实数据 (RPC)
    ├── Page09_ActivityDetailTest.java # ✅ 真实数据 (RPC)
    ├── Page10_PublishActivityTest.java # ✅ 真实数据 (RPC)
    ├── Page11_ServiceListTest.java   # ✅ 真实数据 (RPC)
    └── Page12_ServiceDetailTest.java # ✅ 真实数据 (RPC)
```

---

## 🔑 接口状态汇总

### ✅ 已完成 RPC 集成

| 接口 | 说明 | 数据来源 |
|------|------|---------|
| `GET /api/home/limited-time/list` | 限时专享列表 | xypai-user (users+skills+stats) |
| `GET /api/home/filter/config` | 获取筛选配置 | xypai-user (skills聚合) |
| `POST /api/home/filter/apply` | 应用筛选条件 | xypai-user (users+skills+stats) |
| `GET /api/activity/list` | 组局列表 | xypai-content (RemoteActivityService) |
| `GET /api/activity/detail` | 组局详情 | xypai-content (RemoteActivityService) |
| `POST /api/activity/publish` | 发布组局 | xypai-content (RemoteActivityService) |
| `POST /api/activity/register` | 报名组局 | xypai-content (RemoteActivityService) |
| `GET /api/service/list` | 服务列表 | xypai-user (RemoteAppUserService) |
| `GET /api/service/detail` | 服务详情 | xypai-user (RemoteAppUserService) |
| `GET /api/service/reviews` | 服务评价 | xypai-user (RemoteAppUserService) |
| `GET /api/profile/{userId}` | 对方主页数据 | xypai-user + xypai-content |
| `GET /api/profile/{userId}/info` | 用户资料详情 | xypai-user (RemoteAppUserService) |
| `GET /api/profile/{userId}/skills` | 用户技能列表 | xypai-user (RemoteAppUserService) |
| `GET /api/profile/{userId}/moments` | 用户动态列表 | xypai-content (RemoteContentService) |
| `POST /api/profile/moment/{id}/like` | 点赞动态 | xypai-content (RemoteContentService) |
| `DELETE /api/profile/moment/{id}/like` | 取消点赞 | xypai-content (RemoteContentService) |
| `POST /api/profile/{userId}/follow` | 关注用户 | xypai-user (RemoteAppUserService) |
| `DELETE /api/profile/{userId}/follow` | 取消关注 | xypai-user (RemoteAppUserService) |
| `POST /api/profile/unlock-wechat` | 解锁微信 | xypai-user (RemoteAppUserService) |
| `GET /api/app/order/confirm-preview` | 订单确认预览 | xypai-user (技能信息) + xypai-payment (余额) |
| `GET /api/app/order/preview-update` | 价格预览更新 | 本地计算 |
| `POST /api/app/order/submit-with-payment` | 提交订单支付 | xypai-payment (验密+扣款) + xypai-order (创建订单) |
| `GET /api/app/order/balance` | 获取用户余额 | xypai-payment (RemotePaymentService) |

### ✅ HTTP 直连 (无需 BFF 编排)

| 接口 | 说明 | 直连服务 |
|------|------|---------|
| 发布动态相关 (4个) | 发布动态/上传 | xypai-content |
| 动态详情相关 (8个) | 详情/评论/点赞 | xypai-content |

### ⏳ 待 RPC 集成 (当前使用 Mock)

| 模块 | 接口数 | 说明 |
|------|--------|------|
| 搜索功能 | 9 | 热搜/历史/综合搜索 |
| 首页Feed | 1 | 用户推荐流 |

---

## 📦 RPC 依赖关系

### 已使用的 RPC 接口

| 接口模块 | 方法 | 用途 | 状态 |
|----------|------|------|------|
| `RemoteAppUserService` | `queryLimitedTimeUsers()` | 限时专享 | ✅ 已集成 |
| `RemoteAppUserService` | `getFilterConfig()` | 筛选配置 | ✅ 已集成 |
| `RemoteAppUserService` | `queryFilteredUsers()` | 筛选用户 | ✅ 已集成 |
| `RemoteAppUserService` | `querySkillServiceList()` | 技能服务列表 | ✅ 已集成 |
| `RemoteAppUserService` | `getSkillServiceDetail()` | 技能服务详情 | ✅ 已集成 |
| `RemoteAppUserService` | `getSkillServiceReviews()` | 评价列表 | ✅ 已集成 |
| `RemoteActivityService` | `queryActivityList()` | 活动列表 | ✅ 已集成 |
| `RemoteActivityService` | `getActivityDetail()` | 活动详情 | ✅ 已集成 |
| `RemoteActivityService` | `publishActivity()` | 发布活动 | ✅ 已集成 |
| `RemoteActivityService` | `registerActivity()` | 报名活动 | ✅ 已集成 |
| `RemoteAppUserService` | `getOtherUserProfile()` | 对方主页数据 | ✅ 已集成 |
| `RemoteAppUserService` | `getProfileInfo()` | 用户资料详情 | ✅ 已集成 |
| `RemoteAppUserService` | `getUserSkillsList()` | 用户技能列表 | ✅ 已集成 |
| `RemoteAppUserService` | `followUser()` | 关注用户 | ✅ 已集成 |
| `RemoteAppUserService` | `unfollowUser()` | 取消关注 | ✅ 已集成 |
| `RemoteAppUserService` | `unlockWechat()` | 解锁微信 | ✅ 已集成 |
| `RemoteContentService` | `getUserMomentsList()` | 用户动态列表 | ✅ 已集成 |
| `RemoteContentService` | `likeMoment()` | 点赞动态 | ✅ 已集成 |
| `RemoteContentService` | `unlikeMoment()` | 取消点赞 | ✅ 已集成 |
| `RemotePaymentService` | `getUserBalance()` | 获取用户余额 | ✅ 已集成 |
| `RemotePaymentService` | `verifyPaymentPassword()` | 验证支付密码 | ✅ 已集成 |
| `RemotePaymentService` | `hasPaymentPassword()` | 检查是否设置支付密码 | ✅ 已集成 |
| `RemotePaymentService` | `deductBalance()` | 扣减余额 | ✅ 已集成 |
| `RemoteOrderService` | `createOrder()` | 创建订单 | ✅ 已集成 |

### 待新增的 RPC 接口

```
xypai-app-bff (本模块)
  ↓ Dubbo RPC 调用
├─ xypai-user (RemoteAppUserService) 需扩展:
│   ├─ searchUsers()                # 搜索用户
│   └─ getHotSearchKeywords()       # 热门搜索
│
└─ xypai-content (RemoteContentService) 需扩展:
    └─ searchTopics()               # 搜索话题
```

---

## 🏛️ 微服务架构

```
┌─────────────────┐
│   App 客户端     │
└────────┬────────┘
         │ REST API
         ↓
┌─────────────────┐
│  Gateway (8080) │  ← 统一网关（认证、路由）
└────────┬────────┘
         │
    ┌────┴────────────┬────────────┐
    ↓                 ↓             ↓
┌──────────┐  ┌──────────┐  ┌──────────┐
│ xypai-   │  │ xypai-   │  │ xypai-   │
│ app-bff  │  │ auth     │  │ content  │
│ (9400)   │  │ (9211)   │  │ (9403)   │
└────┬─────┘  └──────────┘  └──────────┘
     │ Dubbo RPC
     ↓
┌──────────┐  ┌──────────┐  ┌──────────┐
│ xypai-   │  │ xypai-   │  │ xypai-   │
│ user     │  │ payment  │  │ order    │
│ (9401)   │  │ (9404)   │  │ (9405)   │
└────┬─────┘  └────┬─────┘  └────┬─────┘
     │             │             │
   ┌─▼───┐      ┌──▼──┐      ┌──▼──┐
   │MySQL│      │MySQL│      │MySQL│  基础设施层
   │user │      │pay  │      │order│
   └─────┘      └─────┘      └─────┘
```

---

## 🚀 快速启动

### 前置条件
```bash
# 1. 确保依赖服务已启动
#    - Nacos（服务注册中心）
#    - Redis（缓存）
#    - MySQL（xypai_user 数据库）
#    - xypai-user (9401) - 用户服务
#    - xypai-content (9403) - 内容服务
#    - xypai-auth (9211) - 认证服务
#    - Gateway (8080) - 统一网关
```

### 运行测试
```bash
# 运行所有页面测试
cd xypai-aggregation/xypai-app-bff
mvn test -Dtest="org.dromara.appbff.pages.*" -Pdev -DskipTests=false "-Dprofiles.active="

# 运行单个测试
mvn test -Dtest="org.dromara.appbff.pages.Page05_LimitedTimeTest" -Pdev -DskipTests=false "-Dprofiles.active="
```

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| `RPC集成计划.md` | RPC 集成的详细技术方案 |
| `RPC集成完成说明.md` | Page05 限时专享 RPC 集成详情 |
| `BFF层RPC改造完整计划.md` | 完整的 RPC 改造计划和时间表 |
| `ruoyi-api/快速理解.md` | RPC 接口定义层文档 |
| `xypai-modules/xypai-user/快速理解.md` | 用户服务实现文档 |

---

**最后更新**: 2025-12-04
**状态**: ✅ 87% 真实数据集成完成 (RPC 12个 + HTTP直连 2个) | ⏳ 搜索功能待改造 (2个)
**新增功能**: 订单确认支付流程 (OrderConfirmController) - 联动 xypai-payment + xypai-order
**下一步优先级**:
1. **Phase 2**: 搜索功能 RPC 集成 (Page06-07) - 唯一剩余的 Mock 模块
2. **Phase 4**: 首页 Feed 流 RPC 集成 - 推荐算法实现

